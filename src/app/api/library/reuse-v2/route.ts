import{NextRequest,NextResponse}from'next/server';import{applyValidationEnvelope,deconstructSource,diagnoseContamination,planCleanup}from'@/v2/domain/source-deconstruction';import{declareProofNeed,extractContiguous,planLongFormWorkflow,planProofMode,proofIntegrityGate,querySemanticRepository,selectProof}from'@/v2/application/proof-and-longform'
export async function POST(r:NextRequest){try{const b=await r.json(),i=b.input;const x=b.action==='deconstruct'?deconstructSource(i):b.action==='diagnose'?diagnoseContamination(i):b.action==='cleanup'?planCleanup(i):b.action==='validation-envelope'?applyValidationEnvelope(i.envelope,i.change):b.action==='proof-need'?declareProofNeed(i):b.action==='select-proof'?selectProof(i.need,i.evidence):b.action==='proof-gate'?proofIntegrityGate(i.evidence,i.recipe,i.facts):b.action==='proof-mode'?planProofMode(i):b.action==='longform-workflow'?planLongFormWorkflow(i):b.action==='extract-contiguous'?extractContiguous(i):b.action==='repository-query'?querySemanticRepository(i.entities,i.query):null;return x?NextResponse.json({data:x}):NextResponse.json({error:'Unsupported reuse action'},{status:400})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Reuse operation failed'},{status:422})}}
