import {NextResponse} from 'next/server';import {authorizeGovernedClient,capabilityParityReport,createTransitionOutbox,deprecationHeaders,enqueuePublicOperation,foundationSeeds,issueWorkspaceSession,presentPublicOperation,runFoundationSmoke,switchWorkspace,verifyWorkspaceSession} from '@/v2/application/platform-foundation';
export async function POST(request:Request){const b=await request.json();try{const a:Record<string,()=>unknown>={seeds:()=>foundationSeeds(b.workspaceId),smoke:()=>runFoundationSmoke(b.input),'issue-session':()=>issueWorkspaceSession(b.identity,b.membership,b.secret,b.now,b.ttlSeconds),'verify-session':()=>verifyWorkspaceSession(b.token,b.secret,b.now),'switch-workspace':()=>switchWorkspace(b.previous,b.next),parity:()=>capabilityParityReport(b.items),deprecation:()=>deprecationHeaders(b.input),'authorize-client':()=>authorizeGovernedClient(b.client,b.input),'enqueue-operation':()=>enqueuePublicOperation(b.type,b.projectId,b.idempotencyKey),'present-operation':()=>presentPublicOperation(b.operation),event:()=>createTransitionOutbox(b.input)};const fn=a[b.operation];return fn?NextResponse.json({data:fn()}):NextResponse.json({error:'unsupported-operation'},{status:400})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'invalid-platform-request'},{status:422})}}
