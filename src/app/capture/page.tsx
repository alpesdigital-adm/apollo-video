import Link from 'next/link'; import { CAPTURE_PROTOCOLS } from '@/v2/domain/capture-direction';
const LABELS:Record<string,string>={clap:'Faça uma palma no início','continuous-screen-clock':'Não pause a gravação da tela','reference-recording':'Guarde a gravação de referência','scratch-audio':'Mantenha o áudio das câmeras','continuous-recording':'Evite pausar as câmeras','visible-player':'Deixe o player visível','screen-recording':'Grave também a tela','continuous-clock':'Mantenha a gravação contínua'};
export default function CapturePage(){return <main className="min-h-screen bg-[#09090b] text-zinc-100"><header className="border-b border-zinc-800 px-6 py-5"><div className="mx-auto flex max-w-6xl items-center justify-between"><Link href="/" className="text-sm text-zinc-400 hover:text-white">← Projetos</Link><span className="text-xs uppercase tracking-[.22em] text-sky-300">Preparar gravação</span></div></header><section className="mx-auto max-w-6xl px-6 py-12"><p className="font-mono text-xs text-zinc-500">ANTES DO UPLOAD</p><h1 className="mt-3 max-w-3xl text-4xl font-semibold tracking-[-.04em]">Dê ao diretor um instante em comum.</h1><p className="mt-4 max-w-2xl text-zinc-400">Arquivos podem começar e terminar em momentos diferentes. Para sincronizá-los com segurança, câmera, tela e áudio precisam compartilhar ao menos um sinal.</p><div className="mt-10 grid gap-4 md:grid-cols-2">{Object.entries(CAPTURE_PROTOCOLS).map(([id,p])=><article key={id} className="rounded-2xl border border-zinc-800 bg-zinc-900/40 p-6"><div className="flex items-center justify-between"><h2 className="text-xl font-medium">{p.label}</h2><span className="rounded-full bg-sky-300/10 px-3 py-1 text-xs text-sky-300">Protocolo</span></div><p className="mt-3 text-sm leading-6 text-zinc-400">{p.explanation}</p><div className="mt-5 border-t border-zinc-800 pt-4"><p className="text-xs uppercase tracking-wider text-zinc-500">Faça isso na gravação</p><ul className="mt-3 space-y-2 text-sm">{p.recommended.map(item=><li key={item} className="flex gap-2"><span className="text-sky-300">✓</span>{LABELS[item]??item}</li>)}</ul></div></article>)}</div><aside className="mt-8 rounded-2xl border border-amber-400/20 bg-amber-300/[.06] p-6"><strong className="text-amber-200">Se nenhum sinal estiver disponível</strong><p className="mt-2 text-sm text-zinc-400">O sistema não fingirá precisão. Você poderá marcar manualmente o mesmo instante em cada vídeo antes da edição automática.</p></aside></section></main>}
