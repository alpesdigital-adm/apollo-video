import {readdir,readFile} from 'node:fs/promises';import {join,relative} from 'node:path';import {fileURLToPath} from 'node:url';const root=fileURLToPath(new URL('../src/v2/',import.meta.url));async function files(dir){const entries=await readdir(dir,{withFileTypes:true});return (await Promise.all(entries.map(x=>x.isDirectory()?files(join(dir,x.name)):[join(dir,x.name)]))).flat()}const forbidden=/\b(?:type|interface|class)\s+(Scene|Asset|Version|Variant|Job)\b/g,violations=[];for(const file of await files(root)){if(!/\.(ts|tsx)$/.test(file))continue;const source=await readFile(file,'utf8');for(const match of source.matchAll(forbidden))violations.push(`${relative(root,file)}: ambiguous ${match[1]}`)}if(violations.length){console.error(violations.join('\n'));process.exit(1)}console.log('Canonical domain language verified');
