# Wave 25 — histórico do gate sintético no editor (até 6 horas)

## Prompt para iniciar no Claude Code

> Trabalhe no Apollo Video V2 por no máximo **6 horas decorridas**, incluindo leitura, implementação, revisão, testes, CI, correções, limpeza e entrega. Crie um worktree e a branch `codex/wave25-synthetic-gate-history` a partir de `0ed9eccfc400ae136380b19cf4abc17af8cdd2c9` (`codex/wave24-synthetic-gate`, PR #57 draft). Enquanto #57 não entrar em `main`, use essa branch como base do novo PR draft; não faça merge ou rebase de branches alheias. Leia integralmente `AGENTS.md`, `CLAUDE.md`, o início de `TODO.md` e Brain #1267–#1270, #2873 e #2909 antes de editar. Confirme `git status`, HEAD, remoto e base do PR. Implemente **somente** o histórico das últimas 20 avaliações em `SyntheticPhaseGatePanel`, usando o GET LIST existente. Preserve autenticação, `EditorReads`, idempotência e o POST da versão atual. Amplie o helper de browser com três avaliações reais persistidas para o mesmo projeto, ao menos duas da mesma versão; use transporte/props controlados para corridas, erros, limite visual de 20 e versão divergente se a fixture real exigir refazer o pipeline. Valide localmente se houver ambiente isolado pronto; caso contrário use o CI isolado existente, dentro do prazo. Pare ao completar seis horas e entregue checkpoint honesto se houver bloqueio. Não acesse Hostinger, não use provider pago/live, não faça deploy, merge ou marque `TODO.md` como concluído.

## Base e resultado esperado

Base conferida em 25/09/2026: PR #57 está draft/open, `codex/wave24-synthetic-gate` em `0ed9eccfc400ae136380b19cf4abc17af8cdd2c9`; `main` em `3c9a2dd79b6bda7c4257c9aa5a8c3197140b20ce`. CI `36173807525` passou em ambos os jobs (Quality 25m29s; Isolated Compose 28m40s), com 2520 testes. O W24 controlado cobriu 5/8 checks, 3/4 critérios, `approved=false`; três checks live permanecem pendentes. Esses números são evidência **da base**, não do Wave 25. Reconfirme todos os refs antes de iniciar, pois podem mudar.

Hoje `src/components/SyntheticPhaseGatePanel.tsx` pede `limit=100`, ordena localmente e descarta tudo exceto o primeiro gate. O resultado deste pacote é um seletor com as **últimas 20 avaliações** do projeto. A avaliação mais recente aparece por padrão e após recarregar a página. Escolher outra avaliação muda, como um único retrato, o resumo, checks, referências, hashes, versão e data. O seletor mostra data, versão e estado; não precisa acrescentar autor à UI. Um gate antigo da mesma versão é identificado como histórico sem ser rotulado como versão obsoleta. Um gate cujo `projectVersionId` **ou** `projectVersionHash` difere das props atuais recebe aviso de versão/hash divergente. Nenhum histórico equivale a aprovação atual.

A API existente `GET /v1/projects/{projectId}/synthetic-phase-gates?limit=20` exige `projects:read`, aceita `limit` entre 1 e 100 e já devolve `gates` em ordem `createdAt desc, id desc` pela consulta PostgreSQL. **Preserve a ordem recebida**; timestamp sozinho não desempata gates criados no mesmo instante. O POST já exige `projects:write` e `{projectVersionId, projectVersionHash}`. Não criar GET por ID, capability, schema, migration, serviço ou endpoint novos.

## Escopo de implementação

1. Guardar a lista recebida e `selectedGateId` no painel, associando ambos à identidade projeto + versão/hash das props. Selecionar o primeiro ID da lista ao carregar/recarregar; manter no máximo 20 itens. Derivar todo o conteúdo visível de um único gate selecionado. Na troca A → B → A, descartar respostas GET/POST antigas e seleção cruzada. Usar os fences já existentes em `EditorReads` e no componente, preservando 401 (login), 403, 429, loading, retry e erros sem mostrar dados do projeto anterior.
2. Exibir seletor acessível das últimas 20 avaliações com data, versão e estado do snapshot. Diferenciar seleção histórica na mesma versão de versão/hash divergente. O estado aprovado/reprovado/incompleto representa somente o snapshot escolhido; a UI deve dizer isso sem sugerir aprovação atual. O resumo, os oito checks, refs, links e `reportFingerprint`/`recordHash` mudam juntos com a seleção. Não fabricar `createdBy` nem dados fora do contrato.
3. O botão explica **“Avaliar versão atual”** e só dispara por clique explícito. O POST sempre usa `props.projectVersionId` e `props.projectVersionHash` atuais, independentemente do item selecionado. Selecionar histórico não cria POST, render, provider, operação ou job. Após sucesso, selecionar pelo ID do gate retornado, inserir ou deduplicar esse gate no histórico e reconciliar com a lista canônica sem permitir que resposta atrasada desfaça a seleção. Após reload completo, voltar ao primeiro gate da ordem do servidor. Preservar a chave de idempotência para retry HTTP incerto da mesma intenção e sua invalidação após sucesso, como já existe.
4. Não alterar API, persistência, contrato, domínio, outras telas, provider, renderer, política do gate, versionamento ou autorização. Não incluir URL deep link, exportação, comparador, dashboard, paginação infinita nem dependência nova.

## Arquivos e provas

| Local | Uso neste pacote |
| --- | --- |
| `src/components/SyntheticPhaseGatePanel.tsx` | Mudança principal: lista de 20, seleção, rótulos e fences. |
| `src/app/_operator/editor-reads.ts` | Ler e preservar o contrato de leitura, retry e fence; se uma correção estrutural ampla for indispensável, registrar bloqueio sem expandir este pacote. |
| `src/v2/ui/synthetic-phase-gate-addresses.ts` | Preservar endereçamento das referências do gate escolhido. |
| `src/app/v1/projects/[projectId]/synthetic-phase-gates/route.ts` e `src/v2/infrastructure/prisma/synthetic-phase-gate-repository.ts` | Conferir contrato e ordenação; permanecer inalterados. |
| `tests/v2/helpers/assert-synthetic-phase-gate-browser.mjs` e `tests/v2/synthetic-wave24-journey.e2e.mjs` | Ampliar a prova de interação no editor autenticado; reutilizar ownership, deadline, screenshot e cleanup existentes. |
| `tests/v2/project-editor-ui.test.mjs` | Guarda estrutural de fonte; não usar como única prova de interação. |
| `docs/quality/synthetic-phase-gate-v1.md` | Registrar alcance e evidência exata do Wave 25, separada da aceitação W24. |

Prova falsificável: persistir pelo caminho canônico **ao menos três avaliações reais do mesmo projeto** e lê-las no browser autenticado, duas ou mais da mesma versão, com ao menos uma diferença observável de cobertura, referências, ID ou hash entre duas delas (por exemplo, antes e depois da evidência que a jornada já produz). Não criar novo backend nem 20 renders. Verificar a ordem recebida, inclusive empate de `createdAt` resolvido por `id desc` no servidor quando o cenário permitir; escolha altera ID, resumo, checks, referências e hashes do mesmo snapshot; reload volta ao mais recente. Se o empate exigir fixture controlada, declarar essa prova como ordenação da UI, sem atribuí-la ao PostgreSQL. O caso de versão/hash divergente deve usar mudança legítima já disponível na fixture; se exigir refazer o pipeline, testar a borda com transporte/props controlados e **não** alegar E2E de mudança real de versão. Usar transporte controlado para corridas/erros e para demonstrar que a UI limita a lista a 20; identificar essas provas como controladas. A jornada PostgreSQL/HTTP existente permanece prova real do caminho de avaliação e deve continuar verde. Capturar screenshot do seletor e do estado histórico, além de contagem de requests que prove zero POST e zero efeito ao apenas selecionar. Testar POST após seleção antiga e inspecionar body: versão/hash das props atuais, nunca os do gate selecionado. Testar sucesso selecionando novo ID, retry com a mesma chave após resultado HTTP incerto, loading/erro/401/403/429, mudança de props de versão e troca rápida A → B → A com resposta atrasada. Não aceitar assert que só encontre texto, nem afrouxar asserções da jornada existente. Um teste unitário novo só cabe se a lógica de seleção for extraída por necessidade real; não adicionar biblioteca de teste.

Comandos já existentes: `npm run test:e2e:synthetic-wave24-journey`, `npm run test:e2e:editor-reliability`, `npm run lint`, `npm run lint:code`, `npm run typecheck`, `npm run build`. A jornada Wave24 só roda de verdade com `APOLLO_SYNTHETIC_WAVE24_JOURNEY_E2E=1`, `APOLLO_WAVE24_RUN_ID`, `APOLLO_WAVE24_EVIDENCE_ROOT` absoluto fora do repositório, `PLAYWRIGHT_CHROME_EXECUTABLE` e `V2_DATABASE_URL` de banco **isolado** com `e2e` no nome, `application_name=apollo-video-e2e-synthetic-wave24-<run>`, `connection_limit=1` e `pool_timeout`/`connect_timeout` até 10. Copiar o setup supervisionado de `.github/workflows/ci.yml` e adaptar somente caminhos/credenciais locais existentes; não inventar segredo nem instalar Docker, Go ou modelos para este pacote. Se PostgreSQL/browser locais não estiverem prontos, usar o CI isolado existente como prova de E2E e reservar tempo para ele. Em execução local, não iniciar PostgreSQL, Next, browser ou túnel sem owner, PID, prazo e cleanup em `finally`; conferir zero backends/processos do run e porta livre no postflight. CI tem de passar nos dois jobs para o SHA do novo PR e publicar a evidência correspondente; status de PR ou teste local não substitui isso.

## Relógio de seis horas

| Minutos decorridos | Entrega da etapa |
| --- | --- |
| 0–20 | Leitura obrigatória, refs, worktree e baseline do componente/contrato. |
| 20–100 | Componente e UI dentro do escopo. |
| 100–160 | Testes focados de seleção, fences e POST atual. |
| 160–220 | Browser/PG local supervisionado, se pronto; caso contrário preparar execução no CI isolado. Screenshots, documentação e cleanup. **Congelar feature até 210 min (3h30)**; depois só correção necessária para passar. |
| 220–265 | Checks, revisão e disparo do primeiro CI **até 270 min (4h30)**. |
| 265–360 | Margem para um ciclo de correção + CI, revisão do SHA e artefatos, cleanup e entrega. |

O tempo é de relógio para **um Claude**, não soma de horas de agentes. Este orçamento é uma estimativa de execução com a base e o ambiente disponíveis; fila ou falha externa gera checkpoint parcial e nunca aprovação por prazo. Na hora 6, parar novas mudanças e novos runs, encerrar processos próprios e registrar commit/PR/SHA, testes que passaram ou falharam, CI pendente/falho, screenshots e bloqueios. Não reduzir asserções para caber no prazo. A entrega pretendida é um PR draft coerente com screenshots, CI integral verde no SHA exato e cleanup comprovado; isso ainda não é aceite do produto. Sem merge, deploy, providers pagos/live, alterações de infraestrutura compartilhada ou `TODO.md [x]` neste pacote.

## Nota histórica de autorização

Os limites de autorização acima registram o escopo original deste plano. Em 25/09/2026, o proprietário solicitou integrar, comitar e fazer merge das Waves 24, 25 e 26, além de validar o E2E em ambiente isolado. Em esclarecimento posterior, determinou que produção só será considerada após a prontidão; qualquer ambiente extra de validação será descartável, com cleanup verificado. A execução continua sujeita aos gates e às regras operacionais do `AGENTS.md`; este plano, por si, não comprova merge, deploy ou aceite.
