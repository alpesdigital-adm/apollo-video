# ADR-134 — Platform foundation and parity

Development infrastructure uses isolated PostgreSQL 16, MinIO S3-compatible storage and the same durable operation/checkpoint model as production. A deterministic vertical smoke crosses upload, normalization, static plan, proxy render and reconstruction with shared trace/workspace/project/job context.

The hosted CI run `30759468720` is the first fully green F0.030 vertical proof. It uses PostgreSQL 16/pgvector and real FFmpeg to cross verified upload, durable ingest, an immutable source-ingest EditPlan, Director planning, an explicit project LUT `none` Command, trusted color compilation, proxy enqueue, worker rendering, promotion, canonical manifest and RenderElementMap. The controlled transcript keeps the test deterministic; local filesystem storage is used by this smoke, so MinIO/object-storage reconstruction and the combined Compose runtime remain separate open gates.

The parallel `Isolated Compose infrastructure` job became green in run `30759955783`: it boots the repository's pinned PostgreSQL and MinIO Compose definitions with fail-closed credentials and loopback ports, migrates a clean database, verifies the expected public-table floor, confirms bucket versioning, performs a byte-exact object write/read and always removes containers, networks and volumes. This proves the isolated database and object-storage provisioning items; it does not yet prove the combined app/worker Compose topology or reconstruction after process restart.

Run `30760710159` extends that gate through the combined supervised topology. After clean migrations and the versioned-object round-trip, Compose builds one V2 image and starts the API plus ingest, render, webhook and long-form workers; the gate verifies API health, every container in `running`, restart policy `unless-stopped`, and unconditional teardown of containers, networks and volumes. The proof exposed a real ESM/CJS import incompatibility that made the long-form worker restart after initial health and now guards that boot contract. It concludes isolated durable-workflow provisioning, but does not claim proxy reconstruction from persisted state after restart.

Run `30763463127` closes the proxy reconstruction gate. The ingest worker promotes the immutable master and editing derivative to a versioned MinIO bucket with full-object SHA-256 metadata and requires a non-null `VersionId`. The test then removes the complete local artifact namespace before proxy execution. A fresh worker relaunch reads the immutable ProjectVersion/EditPlan, source and manifest identities from PostgreSQL, materializes the exact S3 version into operation-owned ephemeral storage, revalidates key, checksum and byte size, renders with real FFmpeg, promotes the proxy back to MinIO and opens it through a second fresh work root to validate its bytes and 540×960/duration probe. The same run passes 801 tests, all media/Remotion goldens, build, Prisma/API integration and a supervised API plus four-worker topology with zero restarts and unconditional teardown. This proves reconstruction in isolated CI; it is not a production deployment or user acceptance.

Local PostgreSQL and MinIO publish only on explicit loopback ports and require
operator-supplied secrets. PostgreSQL uses a dedicated volume and a bounded
Prisma pool. MinIO initializes one declared bucket idempotently and enables
object versioning before the environment is considered ready. A dependency-free
`infra:validate` gate protects these invariants in CI even when Docker is not
available; passing that gate does not claim that containers, migrations or S3
I/O were executed. CI uses that exact pinned PostgreSQL 16 + pgvector image so
the clean migration run cannot silently drift to a server without `vector`.

The disposable local runtime composes that database and versioned bucket with
one immutable application image. A one-shot migration service must complete
before the API and the ingest, render, webhook and long-form workers start.
Every long-running process has PID 1 supervision, restart policy, graceful stop
and shared persistent artifact/work volumes. Static validation proves the
topology contract; it is not evidence that Docker boot or lease recovery ran.

Foundation data is never manufactured as a ready Source. Workspace/client
bootstrap is followed by an idempotent project-source seed that calls the same
project creation and media upload application services as the public API. The
OutputSpec lives in the immutable brief snapshot. A real, rights-confirmed
master must pass byte count and SHA-256 verification, then a durable ingest
operation must finish and persist both source-master and editing-proxy
relationships before the seed reports success. A queued operation alone is not
seed completion.

Deterministic media evidence belongs to CI as explicit jobs rather than the
default unit-test discovery. The hosted workflow runs the editorial renderer,
retimed-transcript worker and two-hour contiguous extraction goldens with real
FFmpeg. Each golden must materialize the current color compilation contract and
assert observable frame, pixel, duration or audio outcomes; a smoke render
alone is insufficient.

Code lint is a separate CI gate from the Apollo architecture boundary scanner.
It uses ESLint 9 with the official Next 16 core-web-vitals and TypeScript flat
configs over application, scripts and tests, and permits no warnings. Stable
Next, Hooks, JavaScript and TypeScript safety rules remain enabled. React
Compiler-only purity/ref/effect/memoization heuristics and the noisy
unused/explicit-any/empty-object rules are excluded until their underlying
patterns can be migrated without a UI rewrite; architecture, typecheck and
domain-language gates continue to cover their respective invariants.

Durable public-operation telemetry is emitted through an application port, not
from domain code. The production repository factory decorates operation
lifecycle writes and emits a closed `public-operation-telemetry/v1` JSON
envelope for creation/replay, claim, heartbeat, phase changes, waiting/resume,
retry, cancellation, success and failure. API-created operations persist the
validated `Apollo-Request-Id` as their trace before crossing the process
boundary; ingest, authorized render, proxy, final export, source cleanup and
long-form workers therefore retain the same trace after claim and replay.
Pre-existing and internal operations retain a deterministic workspace+operation
fallback. The operation ID is the job ID, and project ID is included only when
the persisted operation context has that relationship.
Type, status, phase and attempt are allowed; editorial payloads, file names,
transcripts, URLs and artifact identities are structurally absent. Telemetry is
best-effort and may never change the durable result. Migration, Prisma/API
integration and supervised cross-process runtime passed in hosted run
`30761608326`. Provider/renderer child spans, complete metrics, dashboards and
acceptance remain open under T-NFR-003.

Provider and renderer boundaries use a second closed envelope,
`public-operation-span-telemetry/v1`. The production composition root shares
one sink between lifecycle persistence and worker execution, so normalization,
transcription, long-form stages, proxy/final/authorized rendering and source
cleanup inherit the durable trace instead of minting an unrelated identifier.
Only a bounded span name/kind, deterministic span ID, job/workspace/project,
attempt, terminal result and measured duration are emitted; provider payloads,
paths, transcripts, URLs, artifact identities and exception text are absent.
Hosted run `30762327139` proves the real PostgreSQL+FFmpeg vertical chain emits
started/succeeded pairs for normalization, transcription and proxy rendering
under stable trace/job identities, while unit tests cover failure and collector
isolation. Wait/byte/token/cost metrics, dashboards, alerts and operational
acceptance remain open.

Hosted run `30763965449` extends the closed envelopes without inspecting provider payloads. Lifecycle events measure queue wait at claim and total running duration at terminal settlement. Provider/renderer spans accept only explicitly measured non-negative integer bytes, tokens and minor-unit cost; render and media workers report typed output sizes, while long-form stages report their persisted cost. Invalid measurements are omitted and collector failure remains unable to affect durable work. The production composition root also evaluates deterministic thresholds for failed operations, queue/run/span duration and cost and emits redacted `public-operation-alert/v1` records with canonical IDs, observed value and threshold only. The run passed 804 tests, build, all media/Remotion goldens, Prisma/API integration and the supervised Compose topology. A durable collector/query surface, dashboard and operational acceptance remain open, so F0.030 telemetry is still partial.

Hosted run `30765019248` makes the closed telemetry durable without turning it into an arbitrary event store. Separate PostgreSQL tables persist only the allowlisted lifecycle/span columns and redacted alert columns, use content hashes for idempotency, enforce schema/event/shape/metric constraints, and cascade only with their owning workspace. The public query `apollo.operations.telemetry.summary` requires `operations:read`, binds every aggregate to the authenticated workspace and an exclusive UTC window of at most 31 days, and returns counts plus decimal-string totals/maxima so byte/token/cost precision is not lost. It exposes no trace, job, project, provider payload or error text. PostgreSQL integration proved duplicate replay, workspace isolation, alert storage and exact aggregates; the complete run passed 807 tests, migrations from zero, API integration, build, media goldens and supervised Compose cleanup. Journey/phase dashboards, hard-invariant alerts and operational acceptance remain open, so the F0.030 telemetry checkbox remains partial.

Canonical persisted documents must be compared with canonical serialization,
never JavaScript insertion order. Hosted API integration exposed this at the
project LUT impact boundary after the command-type constraint was corrected:
the stored impact had identical content and hash but alphabetized keys. The
hydrator now revalidates with `stableSerialize`, and a regression test performs
the same canonical persistence round-trip.

ADR-142 selects OIDC Authorization Code + PKCE and opaque server-side sessions for production; the current scrypt bootstrap remains isolated-development only and blocks production until replaced. Workspace switching invalidates caches and subscriptions. Architecture imports are enforced by a CI boundary check. UI actions, REST endpoints and tests map through capability IDs, while sensitive internals have explicit deny-only reasons. Hosted runs `30765404717`, `30766068302` and `30768229810` make the session/tool separation fail closed, prove the login password absent from server logs, persist revocable sessions and distributed throttles, and require the durable session from every current V2 page boundary.

Hosted run `30770605092` integrates the six-destination shell and workspace switching without coupling a human identity to an automation credential. Each active workspace has one server-resolved `V2WorkspaceUiPrincipal`; the human must independently hold an active `V2WorkspaceMember`. A switch validates same-origin state mutation, resolves both records server-side and serializably revokes the old durable session while creating the target session with the original absolute expiry. The browser emits a workspace-changing event, removes only Apollo workspace-namespaced storage/cache entries and performs a hard navigation so prior subscriptions and React state cannot survive. PostgreSQL/HTTP E2E denied expired, cross-origin and suspended-member requests, rejected the old cookie after rotation and returned 404 for the previous workspace artifact while exposing the target artifact. This is integrated CI evidence, not production deployment or acceptance; OIDC remains the deployment gate.

Public operations generalize across ingest, Director, provider, sync, batch, render and export. Public conventions, deprecation/sunset headers, client kill switches and transition outbox events are stable application contracts.

Operation event publication follows the database transition, not worker intent. A shared domain builder derives minimized `operation.status.changed`, `operation.succeeded` and `operation.failed` envelopes, and every covered Prisma writer persists them through the same transaction client that wins the status mutation. Replays, failed compare-and-set attempts and phase-only progress publish nothing. Bulk containment first selects the exact cancelable set, updates it and inserts the full event batch under serializable isolation; a cardinality mismatch aborts the command. This is local implementation evidence until the prepared PostgreSQL journeys and deployment gates run.

The Prisma boundary now has one shared `PublicEvent` outbox mapper. Existing review, artifact lifecycle and client-access transitions use it for `annotation.created`, `annotation.resolved`, `artifact.ready`, `artifact.rejected` and `client.suspended`. These producers publish only after the corresponding write is fenced inside the same transaction and deliberately exclude free text, screenshots and administrative reasons. The catalog's budget threshold event remains planned: estimates or local budget fields are not a sufficient source of truth for a crossing event.

Hosted run `30774673461` establishes the first executable F0.032 logical-boundary gate. The policy defines the permitted dependency direction for Domain, Application, Infrastructure, Public API, Agent, MCP and UI, scans every V2 static import, and separately prevents Web/Editor code from importing generated Prisma, repository factories, storage SDKs or media/database adapters. Only the named public authentication and server UI-session composition roots may assemble infrastructure. The gate exposed and removed two real inversions: ingest and cleanup Application workers now receive probe and file-integrity ports, and the Director agent no longer imports an MCP client type. T-F0.032 also executes a deterministic synthetic path across web, application, domain, orchestrator, provider, critic, compiler and renderer and proves representative violations fail closed. The same run passed the complete PostgreSQL, MinIO, FFmpeg, Remotion, API/OIDC and supervised Compose matrix. This is integrated structural evidence; it does not prove that every product flow already crosses the desired interfaces, that all worker stages are operationally isolated, or that the system is deployed and accepted.

Hosted run `30775230222` replaces the former trace-only fake with typed Application ports. Versioned references now cross Provider Registry/perception, Director, Critic, Compiler and Renderer without exposing an adapter, database or storage location. One orchestrator validates canonical context and every returned reference, propagates the same abort signal, requires a SHA-256-bound compiled RenderInput and fails before Compiler/Renderer when the Critic blocks. The fake vertical test wraps that orchestrator behind synthetic Web, authentication and domain-command calls and checks exact data flow; cancellation and non-canonical input fail before provider selection. The run passed 826 tests and the complete hosted matrix. These contracts are an architecture proof and intended seam, not evidence that the complete production pipeline has already been rewired through them.

Hosted run `30775662600` makes the PRD 10.1–10.6 conceptual inventory executable. Spec 10 maps all 57 normative names to one owner and to 26 typed Prisma targets, five immutable snapshot kinds, fourteen exported value objects or twelve explicit planned gaps. The test derives the expected set from the PRD, rejects missing/duplicate rows, validates every model and exported symbol, checks snapshot kinds and prevents a generic conceptual/entity/document table. It also removes the previous partial in-code registry so the PRD plus Spec 10 are the only inventory. The run passed 827 tests and the full hosted matrix. This exposes rather than closes the twelve missing contracts; relational graph verification, implementation, deployment and acceptance remain open.

Hosted run `30776437393` validates the five central contract seams against Specs 02, 03 and 06. Source media crosses the domain through content-addressed manifests and rights/lineage identities, never a permanent URI; timeline timing remains semi-open, frame-first and positive-rate; OutputSpec validates canvas, ratio, fps and normalized safe areas; EditCommand requires exact base/hash, actor, non-empty scope, idempotency and a registered invalidation policy. The obsolete domain ProviderAdapter stub was removed. Its Application replacement exposes versioned adapter identity, TTL-bound capabilities, estimate-before-submit, idempotent submit context, status, retrieve, optional cancellation and verified webhook without any credential field; runtime validation rejects schema drift, secret-like extras, duplicates, unsupported operations and invalid TTL/duration. The run passed 832 tests and the complete hosted matrix. DocumentAsset support and a durable real-provider journey remain open, so this is compatibility evidence rather than completion of the provider subsystem.

Hosted run `30776962496` closes the target-version declaration task. `config/platform-versions.json` is the sole machine-readable inventory for Node, Next/React, Remotion, target FFmpeg/ffprobe, PostgreSQL/pgvector, Prisma, MinIO and key client libraries. The CI gate checks exact manifests and lock entries, pinned container images and matching declarations in ADR-001/002/008. It removed the stale Next 16.2.10 foundation constant and records 16.2.12, while explicitly treating FFmpeg/ffprobe 8.1.1 as targets rather than pretending every current environment already runs them; media probes and goldens remain the behavioral gate. Both dependency audits reported zero vulnerabilities, 832 tests and the complete hosted matrix passed.

The Next.js/React supported-line upgrade is also closed. Official support documentation checked on 2 August 2026 identifies Next.js 16.x as Active LTS and React 19.2 as the current documented line, including the published 19.2.7 patch used by both the app and renderer. Commit `c350181` performed the controlled upgrade and async request API codemod; the current baseline is Next 16.2.12. The first closure run exposed an intermittent but real concurrent-session defect: one of two OIDC session refreshes returned `503` after losing the compare-and-set, because that expected race bypassed the Prisma-code retry path. Commit `c491c9c` gives the in-process conflict a bounded backoff/retry, makes recovery of the already-created deterministic successor read-only and raises the PostgreSQL test to eight concurrent refreshes. Hosted run `30778010953` regenerated App Router types, typechecked the asynchronous dynamic params, validated all 189 public capabilities without baseline drift, bundled and smoke-rendered Remotion with real Chromium, built the production application and passed OIDC/API integration plus the complete PostgreSQL, MinIO, FFmpeg and supervised Compose matrix. This closes compatibility of the upgrade, not the remaining F0.033 model/provider/storage work or production acceptance of those separate capabilities.

Hosted run `30778629569` closed the named non-major dependency advisories with both a live registry audit and a deterministic lockfile gate. Later registry updates classified PostCSS through 8.5.22 under `GHSA-fxqj-rqcc-2cmp`, `fast-uri` 3.0.0 through 3.1.4 under `GHSA-7p8r-x3mc-p8w7`, Hono before 4.12.34 under `GHSA-8j4g-w8fx-2239`, and `ip-address` through 10.3.0 under `GHSA-mwp4-54f8-5fhr`, `GHSA-4xrf-jv44-h6hh` and `GHSA-22jq-vg5j-6vgg`. The baseline therefore pins patched `postcss` 8.5.25, `fast-uri` 3.1.5, Hono 4.12.34 and `ip-address` 10.3.1 in their applicable root/Remotion locks and overrides, while `uuid` remains 11.1.1 and `form-data` remains absent from both trees. Future dependency changes fail CI if they silently reintroduce one of these resolved conditions, independently of registry advisory availability.

Hosted run `30807744785` closes the F0.033 S3-compatible configuration and PostgreSQL-only persistence tasks. The current composition root selects S3 explicitly; the isolated topology initializes a pinned, loopback-only MinIO service and a required versioned bucket before starting the API and workers. The vertical smoke promotes immutable masters and derivatives, deletes all local artifact state, reconstructs and renders the proxy from PostgreSQL plus exact S3 key, SHA-256, byte size and VersionId, reopens the result from a fresh work root and tears the topology down. S3 reads remain workspace-revalidated, authorization-bounded and non-public. PostgreSQL was already mandatory at client creation and in the V2 Prisma datasource; commit `3aba0f9` makes this architectural: the CI gate rejects SQLite imports, package dependencies and datasource drift, with falsification tests for both dependency and schema mutations. The run passed 832 tests, clean migrations, API contracts, real media/Remotion checks and both supervised jobs.

Hosted run `30809793462` closes the central relational-graph documentation and integrity task. Spec 10 now lists executable child relation, FK tuple, parent unique tuple and delete policy for the Project/Version/Snapshot/Director and media ingest/render/review/export path. T-F0.033 parses that table against Prisma, requires every target tuple to be primary or unique, proves every documented aggregate reaches Workspace and falsifies both cross-project and cross-artifact substitutions. Migration `20260803130000_central_reference_graph` adds project/workspace-aware FKs and artifact/manifest bindings. The first run correctly rejected an ingest operation because its source IDs are deterministic reservations for outputs not yet materialized; the corrected decision keeps future output reservations scalar and enforces their identity when the terminal attempt is persisted, while all materialized inputs remain FK-bound. The final run applied 137 tables, 677 indexes and 525 FKs from zero, passed 833 tests, vertical PostgreSQL/MinIO reconstruction, build, Remotion, Prisma/API integrations and complete teardown.

Hosted run `30810405231` closes the F0.034 PublicCapability registry task. The canonical registry contains 189 immutable capabilities and carries exposure, operation kind, scopes, input/output schema references, cost class and confirmation policy together with endpoint/auth/idempotency metadata. Commit `6a96e39` adds runtime allowlists and canonical patterns for those dimensions so JavaScript callers, generated data or future refactors cannot bypass the contract merely because the TypeScript interface compiled. High/variable-cost operations still require a gate, every schema reference resolves through the public schema registry and T-F0.034 falsifies each metadata dimension. The full hosted matrix passed 834 tests, the unchanged public baseline, build, PostgreSQL/MinIO, media/Remotion, Prisma/API integrations and teardown. This closes registry existence and validation; UI-action coverage and same-handler parity remain separate open tasks.

F0.034 parity is derived from executable structure, not a hand-maintained UI list. A TypeScript AST scanner covers every TSX component and client-marked TypeScript helper, recognizes literal/template `/v1` calls and explicitly classifies the signed upload transfer while rejecting unknown dynamic fetches. Method plus canonical endpoint shape binds each of the 73 current call sites to exactly one of 66 exposed capabilities. The route resolver then traverses local and imported call graphs until it reaches V2 Application services; because both the UI and external clients enter through that same `/v1` handler, a parallel UI-only mutation path fails the gate. Hosted runs `30811367434` and `30811899810` prove the binding and shared-service boundaries.

Internal-only status is a typed deny decision. Six canonical surfaces record category, durable reason and forbidden public schema keys. Arbitrary reason strings, duplicate or malformed entries, public capabilities claiming an internal exception and internal capabilities without an allowlisted justification are invalid. Hosted run `30812369531` proves those falsifications and absence from all 339 public schemas.

The deterministic parity inventory is generated as `ui-capability-parity-report/v1`. It records UI source action, capability/version, endpoint, reachable Application services and the structural/public-contract tests for every row, plus coverage of every registered capability and the internal allowlist. `api:parity:validate` compares the committed report byte-for-byte and is a mandatory CI step beside `api:v1:validate`. Hosted run `30812924567` passed 837 tests and the complete build, PostgreSQL/MinIO, media/Remotion, Prisma/API and teardown matrix with 189 public contracts, zero unjustified capabilities, zero unbound UI actions and zero routes outside the Application boundary. This closes F0.034 as an integrated architecture gate; it does not substitute for the product-level deployment and acceptance evidence owned by each capability's functional requirement.

F0.035 moves public conventions out of the Application placeholder and into the Public API boundary. The machine-readable contract fixes `/v1`, closed UTF-8 JSON, opaque IDs, RFC 3339 UTC, non-negative frame indices with half-open intervals, stable cursor pagination and capability-declared filters. Presenters and common ID/date schemas import the same constants. Authenticated routes validate query names, duplicates and required parameters after authentication but before invoking Application; signed upload/download routes apply the same allowlist without pretending that their signed token is a human or agent credential.

The registry is also the route source of truth. T-FR-241 derives all 189 exported HTTP handlers from the App Router, requires exactly one matching capability and checks every statically read query parameter against capability metadata. This found that the MVP Core list accepted `limit` without publishing it; capability 1.1.0 and the baseline add only that optional parameter. Hosted run `30815108018` then found a second real defect: literal `/operations/dead-letter` also matched dynamic `/operations/{operationId}` and changed an authorization failure into a parity error. Route resolution now chooses the most literal compatible template and rejects only equal-specificity ambiguity. Final run `30815677386` passed 841 tests and the complete PostgreSQL/MinIO, API/OIDC, media/Remotion, build and teardown matrix. This closes the convention and source-of-truth tasks, not the remaining F0.035 error, example, evolution, deprecation and per-operation contract tasks.

Public errors are now catalog-first. The immutable registry covers all 116 DomainError codes plus `INTERNAL_ERROR`, with explicit HTTP status, category, retryability and a safe canonical client message. Missing or duplicate classification fails closed; error-envelope v3 exposes the exact code/category vocabulary while keeping v1/v2 published. The presenter never echoes provider/domain diagnostics, and the generic 500 path reads the same catalog instead of maintaining a second envelope. Hosted runs `30818147820`, `30818838812` and `30819557419` progressively proved HTTP category semantics, diagnostic redaction and the complete fallback catalog through the full matrix.

Documentation is a build product of the contract source. `api:v1:docs:build` emits deterministic OpenAPI, every versioned schema and a content-addressed manifest after all 385 examples pass Ajv Draft 2020-12. Contract evolution is baseline-complete: the compatibility detector distinguishes additive changes from breaks, but any new capability or schema must be deliberately recorded so it cannot later disappear unnoticed. The gate first rejected the untracked error-envelope v3, then accepted a reviewed baseline diff containing only that schema. Runs `30820090576` and `30820627232` passed publication and compatibility.

Deprecation is schema-specific and fail-closed. The immutable registry requires canonical UTC dates, at least 180 days of support and a local migration guide; the schema route formats RFC `Deprecation`, `Sunset` and deprecation-link headers only for the registered old version. The guide is included in the content-addressed bundle and only registered guide paths bypass UI authentication. The first hosted HTTP assertion exposed the missing proxy allowlist instead of accepting a login page as documentation; `ece1bab` corrected it without exposing an arbitrary directory. Contract coverage is exhaustive rather than aggregate: one generated subtest for each of 189 capabilities proves its executable handler, Application boundary or one of four closed infrastructure boundaries, OpenAPI/version/auth/scopes/idempotency/parameters/status/media types, validated schemas/examples and v3 errors. Run `30823898229` passed 1,035 tests, the 343-file bundle with hash `2d29b3a0...`, PostgreSQL/MinIO, API/OIDC, media/Remotion, build and teardown. This closes F0.035 as an integrated contract gate; it does not claim functional deployment or acceptance for unrelated capabilities.

External authorization now has one canonical identity model. `ApiClient` schema v2 owns type, canonical scope grants, an explicit allowed-environment set and creator identity; `ServiceAccount` is constructed through a dedicated factory and `ApiCredentialRef` contains identifiers only. PostgreSQL stores those fields directly and the migration removes the superseded singular environment/scope columns, so neither Bearer nor human-session authentication can fall back to parallel authority. Public administration moves to response schemas v2 while retaining v1 schemas only under the public contract lifecycle. Hosted run `30827500404` passed clean migrations, 1,036 tests, parity/contracts, real PostgreSQL/HTTP integration and the complete build/media matrix. This decides the F0.036 identity shape; credential issuance/storage, full authorization matrix, audit and kill-switch completion remain governed by ADR-010/013 and their open tasks.

The authorization vocabulary is now closed as well. `API_SCOPE_MATRIX` owns the 13 resource/action grants used by the current capability registry; syntactically plausible unknown grants are invalid at client hydration, registry definition and runtime guard boundaries. A structural T-FR-242 gate requires the matrix and capability union to be identical, resolves each authenticated capability to its route and shared Application services, and finds the declared `requireScope` enforcement there. Adding authority therefore requires one reviewed matrix+capability+enforcement change. This is local evidence until hosted PostgreSQL/HTTP, deployment and acceptance run.

Authentication output is now tamper-evident at the Application boundary. Bearer and UI session actors carry one frozen `ExternalAuditContext`; the scope guard checks client, credential, workspace, environment and the complete optional delegation tuple against it before evaluating authority. UI delegation originates only from the durable member/session and Bearer cannot supply it. Authenticated grants use an immutable set implementation without mutation methods, closing the runtime escalation possible with a TypeScript-only `ReadonlySet` backed by a normal mutable `Set`. A complete audit ledger, hosted proof, deployment and acceptance remain separate gates.

Sensitive containment commands now carry durable authentication attribution without widening their public projection. The repository stores credential or UI-session identity, environment, authentication kind, the complete optional delegation tuple and a canonical context hash. That hash participates in the idempotency fingerprint and is recomputed during hydration, so a different delegated human behind the shared UI client cannot replay another administrator's command. Historical pre-contract commands cannot be truthfully attributed; the pre-production migration deliberately truncates them before enforcing non-null audit columns. Other capability families still need equivalent ledger coverage before the audit-context task can close.

Durable operation creation now uses the same attribution rule across materialization, render, ingest, proxy, final export, Director, source cleanup and long-form indexing. The Application boundary rechecks the capability scope, fingerprints the canonical context hash and stores the tuple on `PublicOperation`; source cleanup and long-form also store it on their own aggregate and require both hashes to match. Effective cancel/retry transitions append one `PublicOperationControlCommand` atomically with state and outbox, while convergent no-op calls append nothing. Pre-contract rows keep a nullable all-or-none group solely so the migration does not fabricate identity; hydration rejects that group until the authorized pre-production reset. PostgreSQL/HTTP execution, deployment, acceptance and remaining ledgers keep F0.036 open.

Batch production mutations use the same rule as one closed family. Thirteen public mutations across production batches, retries, alignments, take libraries, compatibility graphs, variant compilation, portfolio preflight and batch edits must carry `AuthenticatedExternalActor` through the route and Application boundary, recheck `projects:write`, and bind idempotency to the canonical context hash. Eleven immutable aggregate/command rows store the complete tuple. Replays, concurrent-winner recovery and edit preflight commits require the exact same identity; hydration rejects missing or tampered tuples. The migration deliberately avoids historical identity backfill, and a structural gate enumerates every participating route, service and Prisma repository. Local tests pass and PostgreSQL/HTTP journeys are prepared; hosted execution, deployment and acceptance remain open.

Project-intelligence mutations form another closed audit family. Speech, evidence and validated-segment catalogs; semantic documents, evaluations, reuse and scale reports; source deconstruction; and contamination diagnosis carry one `AuthenticatedExternalActor` through nine public routes and six persistence adapters. The Application boundary rechecks `projects:write`, and the canonical context hash scopes idempotency, replay and concurrent-winner recovery. Nine aggregate heads store the complete tuple and hydration recomputes it. Historical rows are not assigned invented identities: the database group remains all-null or complete, while runtime reads fail closed. Structural and local adapter tests pass; the source-deconstruction and contamination PostgreSQL/HTTP journeys are prepared but unexecuted, so deployment and acceptance remain open.

Externally initiated editorial Commands are one more closed audit family on the existing `V2EditCommand` aggregate. Spoken-content removal, transcript replacement, manual edit/restore, individual and batch review-patch application, and comparison accept/reopen carry the complete `AuthenticatedExternalActor` through six Application services and six Prisma adapters. The boundary rechecks `projects:write`; the context hash scopes fingerprints, replay, and concurrent recovery; hydration rejects missing, tampered, or cross-credential attribution. A corrective migration replaces the prior PostgreSQL constraint with explicit non-null predicates for every required external field, because a `CHECK` must not admit a partial tuple through `UNKNOWN`; it performs no identity backfill. Director-worker Commands remain outside this external family until their durable worker provenance is modeled rather than mechanically borrowing the request actor. Local structural/unit tests and the real worker/FFmpeg golden pass; PostgreSQL execution, deployment, and acceptance remain open.

Client and credential administration now follows the same attribution contract through an immutable `ApiAdministrationCommand`. Client creation and credential rotation commit identity, one-way verifier, idempotency result and audit command together; the actor-context hash is part of the request fingerprint. Credential revocation now commits the terminal credential state and exactly one command in a serializable transaction. Commands never contain the bearer secret. Pre-contract admin replays and revoked credentials are intentionally removed because no trustworthy actor tuple can be reconstructed. Hosted migration/API proof and the remaining capability families keep F0.036 open.

Webhook endpoint, subscription, signing-secret and replay administration now uses the same rule through `WebhookAdministrationCommand`. All eleven mutations, including challenge, accept the authenticated actor rather than a caller-supplied client ID, enforce `webhooks:admin` at the shared Application boundary, and bind actor-context hash, target, endpoint/status/base when applicable. Prisma commits the command atomically with creation/idempotency, replay effect or lifecycle CAS. Challenge performs its external proof before the transaction but records authorship only with successful verification and endpoint activation; an active follower must match that durable command. Concurrency and response-loss cannot fabricate a different actor. Tokens, signing material and ciphertext never enter the ledger. Hosted PostgreSQL/deploy evidence is still required.

Media-artifact lifecycle transitions use that same binding. The Application service owns `artifacts:write`, workspace hiding and audit materialization; idempotency is scoped by the canonical actor-context hash instead of only the technical client. Prisma stores the complete tuple in the immutable transition, validates it on hydration and commits it with the revision fence, state change and outbox. Pre-contract transitions and their replay records are deliberately removed because their credential or delegated member cannot be reconstructed truthfully. This is local evidence until the isolated PostgreSQL/HTTP gate, deployment and acceptance run.

Short-lived media download grants now bind both ends of their lifecycle. Issuance persists the complete actor tuple and scopes its fingerprint to the context hash without persisting the bearer download token. Revocation atomically writes terminal state plus a separate revoker tuple; convergent replay is accepted only for that exact context hash. Both tuples are reconstructed and hash-checked on hydration. Existing pre-contract grants are invalidated because their true credential/member cannot be recovered. The final-export HTTP/PostgreSQL journey is prepared but remains unexecuted in the hosted gate.

Media-upload mutation identity is an immutable append-only ledger covering begin, session issuance, part recording, completion and abort. Every state mutation and its audit entry commit in one Prisma transaction. A scalar pointer identifies the current session entry; the repository, rather than a cyclic database foreign key, validates the exact upload, workspace, action and actor binding. Reissuing a session invalidates older signed PUT tokens before storage access. Terminal replay is convergent only for the same canonical context. Pre-contract uploads fail closed because their true identity cannot be reconstructed. Local tests pass and a PostgreSQL integration is wired into CI, but its hosted run, deployment and acceptance remain pending.

Asset-rights authorship belongs to an immutable `AssetRightsChange` per revision, not to the content-addressed snapshot. A single snapshot may truthfully be selected by several revisions, so each change independently binds base/result revision, snapshot, fingerprint, timestamp and either a complete external audit tuple or an explicit internal origin. The public Application boundary owns `artifacts:rights` and actor materialization. Exact response-loss replay requires the same context hash; another credential creates a distinct revision even when the snapshot bytes are reused. Current-rights hydration requires the matching change and rejects tampering. PostgreSQL evidence is wired into CI but remains unexecuted in the hosted gate.

Project creation and copy-on-write duplication now share an immutable `ProjectCreationCommand` ledger. The command distinguishes `create` from `duplicate`, binds the resulting Project and ProjectVersion, and requires exact source Project/ProjectVersion lineage for duplicates. The Application service materializes the complete authenticated tuple, includes its context hash in idempotency, and commits command plus result atomically. Replay recomputes command identity and rejects missing, tampered, or cross-credential evidence. Operational seeding must name a real credential and environment instead of synthesizing authorship. Local tests and PostgreSQL journeys are prepared; hosted execution, deployment, acceptance, and remaining mutation families keep F0.036 open.

Workspace and project LUT mutations now use the same attribution boundary as one coherent family. Import, immutable version creation, revision-fenced lifecycle, and revision-fenced workspace default persist the authenticated tuple in their immutable records. Project selection keeps `V2EditCommand` as the sole command aggregate: external calls add credential/environment/kind/delegation/context hash to that row, while trusted Director/system work has an explicit internal author and no fabricated authentication. The hash scopes idempotency, hydration recomputes it, and public responses remain redacted. Pre-contract LUT projections are reset; pre-contract external EditCommands without exact attribution fail closed rather than receiving a backfill. Local adapter tests and the PostgreSQL/HTTP journey are prepared; hosted execution, deployment, and acceptance remain open.

The aggregate F0.036 security journey now has a prepared HTTP/PostgreSQL falsification matrix for delegated-role scope denial, cross-workspace hiding, explicit bearer expiry, zero-overlap rotation, replaced-token denial, credential revocation and recoverable containment. Expired credentials must fail before `lastUsedAt`; self suspend/revoke must fail before an access command is written. This prepared test is not completion evidence until it runs in the isolated hosted gate and the resulting version is deployed and accepted.

Containment recovery is human-role gated. The shared UI principal's technical grants do not grant admin authority to every delegated member: `clients:admin`, `webhooks:admin` and the four recovery capabilities require a durable `administrator` role. Unknown roles fail authentication. Suspend and revoke cannot target the client authenticating the same request because that transition removes the recovery channel; engaging and releasing its kill switch remains allowed and fenced. Existing CAS, command audit, operation cancellation and outbox atomicity are preserved. Hosted PostgreSQL/HTTP, deployment and acceptance remain open.

The service-account Bearer boundary is strict and runtime-stable. Issuance validates identifiers before entropy use, generates a 256-bit base64url secret plus independent 128-bit salt and persists only a 32-byte `scrypt` derivation. Current parameters are pinned to the compatible Node profile (`N=16384`, `r=8`, `p=1`, bounded memory); changing them requires an explicit credential version and migration. Parsing requires the exact four-segment shape, authentication caps and validates the header, and malformed persisted crypto returns the same `AUTH_INVALID` boundary. Verification uses `timingSafeEqual`, while expiry and last-use share one application-clock instant. Hosted run `30829124000` passed the complete PostgreSQL/HTTP and platform matrix. This closes emission/validation only, not the remaining credential-reference, authorization, audit or kill-switch tasks.

Credential ownership has completed its contract phase. The initial expansion created independent `ApiCredential` rows and backfilled verifiers; migration `20260803190000_contract_api_credential_verifiers` now removes the redundant verifier columns from `ApiClient`, narrows the credential salt/hash columns to their canonical lengths and enforces the salt format. A database gate inspects the final Prisma model and committed migration so identity-level copies cannot return. Create and rotate persist only a one-way verifier plus idempotent IDs; only the first committed response receives the generated token, and response-loss or concurrent replay cannot recover it. Rotation overlap and convergent revocation remain credential-scoped. Hosted run `30830871011` passed 1,037 tests, clean migration, real PostgreSQL/API flows and the full platform matrix. This closes one-shot/reference/rotation/revocation, not authorization, audit, kill switches or the aggregate security E2E.

Public operation cost is source-bound. A generic cost class or telemetry sample is never promoted into an operation estimate. The first trustworthy source is the persisted long-form workflow: its canonical stage budgets produce `estimatedCost`, its workflow budget produces the maximum, and its persisted checkpoint summary produces `actualCost` only after a terminal operation state. Hydration revalidates canonical workflow JSON, hash identity and scalar cost columns before exposing either projection. Operations without an equivalent reservation or measurement omit cost, and retry clears a prior terminal actual cost until a new terminal measurement is committed. This local slice evolves generic operation contracts to v10 and long-form contracts to v3; hosted PostgreSQL/API evidence and other operation types remain open.

Webhook administration follows the same parity boundary. The settings UI is a signal-oriented control room over the public `/v1` contracts, not an infrastructure console: 15 reads and reversible commands bind to registered capabilities and reach V2 Application services through the generated parity gate. Independent collections load concurrently, lifecycle commands carry revisions, effect creation/replay carries idempotency, and attempt history is read from persisted diagnostics. A disclosed signing secret exists only in React memory and is explicitly discarded; no browser persistence or second retrieval path is introduced. Terminal revocation and broad hygiene remain API-only behind their stronger confirmation policies. This is local structural and component evidence until browser plus PostgreSQL E2E, deployment and acceptance run.
