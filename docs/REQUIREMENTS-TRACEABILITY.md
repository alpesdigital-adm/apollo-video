# Apollo v2 — Matriz de rastreabilidade

> **Fonte:** PRD v1.2  
> **Objetivo:** garantir que cada requisito tenha fase, spec, dependência, evidência de aceite e teste.

## Legenda

- **F0:** Fundação.
- **F1:** MVP Core.
- **F2:** Lotes/reuso/formatos.
- **F3:** Sintético/transformação.
- **F4:** Multicâmera/long-form avançado.
- **F5:** Localização/áudio.
- **S1:** Diretor/qualidade.
- **S2:** EditPlan/versionamento.
- **S3:** Biblioteca de mídia.
- **S4:** Lotes/compatibilidade.
- **S5:** Multicâmera.
- **S6:** Providers sintéticos.
- **S7:** UX/editor.
- **S8:** Localização/áudio.
- **S9:** API externa/automação.
- **D0:** domínio/banco; **D1:** mídia/storage; **D2:** Director/perception; **D3:** EditPlan/Commands; **D4:** jobs/providers; **D5:** renderer; **D6:** UI; **D7:** rights/policy; **D8:** sync; **D9:** localization/audio.
- **D10:** public API, autenticação externa, eventos e automação.

Cada teste recebe ID `T-<FR>` no test plan da fase.

## F0 — Fundação

| Req | Título | Spec | Dep. | Evidência de aceite | Teste |
|---|---|---|---|---|---|
| FR-001 | Workspace | S7 | D0,D7 | isolamento e settings persistidos | integration/e2e |
| FR-020 | Brand Kit opcional | S7 | D0,D1 | kit vazio e configurado resolvem corretamente | integration |
| FR-022 | Guardrails estruturados | S1 | D0,D7 | policies compiladas e aplicadas | policy |
| FR-023 | Precedência | S1 | D7 | conflitos resolvidos na ordem definida | unit/policy |
| FR-024 | Policy Snapshot | S2 | D0,D3,D7 | versão mantém snapshot imutável | integration |
| FR-031 | Masters imutáveis | S3 | D1 | derivado não sobrescreve master | integration |
| FR-032 | Content addressing e deduplicação | S3 | D1 | upload repetido reutiliza checksum | integration |
| FR-033 | Normalização com lineage | S3 | D1,D4 | derivado aponta para parent/recipe | integration |
| FR-034 | Preservação de timebase | S5 | D1,D8 | PTS/timebase persistem antes do normalize | fixture |
| FR-035 | Direitos | S3 | D0,D7 | unknown/restricted bloqueiam usos proibidos | policy |
| FR-041 | Tipos de ativos | S3 | D0,D1 | schemas aceitam todos os kinds | schema |
| FR-070 | EditPlan versionado | S2 | D0,D3 | schemaVersion e migration obrigatórios | unit/migration |
| FR-071 | Tracks | S2 | D3 | tracks compilam sem acesso ao banco | golden |
| FR-072 | Source ranges | S2 | D1,D3 | source↔timeline preserva frames | property |
| FR-073 | Múltiplas fontes | S2 | D1,D3 | plano referencia N sources | integration |
| FR-074 | Commands/Patches | S2 | D0,D3 | user/IA geram mesma transação | integration |
| FR-075 | Protected elements | S2 | D3,D7 | Director não altera protected | policy |
| FR-076 | Dependency graph | S2 | D0,D3,D4 | invalidation set é determinístico | unit |
| FR-161 | OutputSpec | S2 | D0,D3 | ratio/resolução/safe area separados | schema |
| FR-162 | Plano canônico e variantes | S2 | D3 | story compartilhada, layouts isolados | integration |
| FR-220 | ProjectVersion | S2 | D0,D3 | toda mudança cria versão | integration |
| FR-221 | Fork copy-on-write | S2 | D0,D1,D3 | fork não duplica masters | integration |
| FR-222 | Isolamento | S2 | D0,D3 | fork não altera original/status externo | e2e |
| FR-223 | Diff e restore | S2 | D3 | diff semântico e restore auditável | integration |
| FR-224 | Artifact lineage | S2,S3 | D0,D1,D3 | render rastreia inputs/jobs | integration |
| FR-232 | Durable jobs | S6 | D4 | restart retoma job idempotente | resilience |
| FR-233 | Partial invalidation | S2 | D3,D4 | mudança local enfileira só dependentes | integration |
| FR-234 | Props/manifest | S2 | D3,D5 | manifest reproduz RenderInput | golden |
| FR-236 | Estados | S7 | D0,D4,D6 | transições válidas e visíveis | state/e2e |

Atualização local de FR-236 — `PublicOperation` agora possui a projeção fechada `visible-state/v1`: queued, in-progress, waiting, retry-scheduled, completed, failed e canceled mapeiam para tone, progresso honesto, ação primária, ações permitidas e terminalidade. Progresso running só é percentual quando `total` existe; waiting/retrying são indeterminados. As transições server-side `running → waiting → running` preservam attempt/progresso e rejeitam retomada para fase anterior. O adapter Prisma exige a lease vigente para entrar em espera, limpa owner/heartbeat/expiração e retoma sob CAS de workspace/status/attempt, adquirindo uma nova lease no mesmo commit sem incrementar attempt. As capabilities `apollo.operations.list`, `dead-letter.list`, `read`, `cancel` e `retry` permanecem em v7, pois a mudança é interna ao worker. Testes locais cobrem os sete estados, estado inventado fail-closed e o adapter controlado; o caso PostgreSQL foi preparado, mas não executado. Writers futuros, deploy e aceite permanecem pendentes.

Atualização local de batch em FR-236 — `ProductionBatch` e cada item agora derivam estado visível dos steps persistidos. Um lote com itens concluídos e falhos apresenta `partially-failed`, percentual baseado apenas em steps concluídos, resultados válidos preservados e retry dirigido às falhas. Seis capabilities que retornam o agregado evoluíram para respostas v2; os contratos v1 continuam publicados. Projeto, relação stale de versão, artifact, PostgreSQL E2E, deploy e aceite permanecem pendentes.

Atualização local de stale em FR-236 — `apollo.projects.artifact-invalidations.read` v2 apresenta a invalidation pendente como `stale-output`, declara `availabilityEffect: none` e separa reconstrução da abertura do artifact histórico. Testes locais cobrem tamper fail-closed e as resoluções exatas de proxy/final já persistidas; o aggregate artifact não é mutado. Estado agregado de projeto/artifact, PostgreSQL E2E, deploy e aceite permanecem pendentes.

Atualização local de artifact em FR-236 — `apollo.artifacts.read` v3 projeta o lifecycle persistido `available|quarantined|deleted` sem percentual e sem confundi-lo com `stale-output`. Adapter Prisma e presenter usam a mesma allowlist de domínio; testes cobrem os três estados, ação/tone/terminalidade, imutabilidade e estado inventado fail-closed. Estado agregado de projeto, transições de lifecycle, PostgreSQL E2E, deploy e aceite permanecem pendentes.

Atualização local de Project em FR-236 — as 14 fases persistidas possuem uma projeção canônica e fail-closed. `apollo.projects.list` v2 e `apollo.projects.create` v3 vinculam status, label, tone, progresso, ação e terminalidade em schemas aditivos; versões anteriores continuam publicadas. Testes percorrem todas as fases, rejeitam status inventado e falsificam label/ação incompatíveis. A cobertura de workspace foi adicionada no incremento seguinte; matriz canônica das transições persistidas, estado de versão, PostgreSQL E2E, deploy e aceite permanecem pendentes.

Atualização local de workspace em FR-236 — `apollo.projects.workspace.read` e `apollo.projects.workspace.current.read` v6 agora convergem no mesmo `project-workspace/v7` completo, incluindo Project, versão corrente e operações aninhadas com projeção visível. A hidratação Prisma rejeita status persistido desconhecido. Testes validam ambas as capabilities, o schema novo e adulteração de status/label; versões anteriores permanecem publicadas. PostgreSQL E2E, respostas editoriais ainda não evoluídas, deploy e aceite permanecem pendentes.

Atualização local de transições Project em FR-236 — uma matriz exaustiva governa as 14 fases, repetição convergente e terminais. Todos os writers atuais de status (ingest, proxy review, compare e export final) incluem no mesmo `UPDATE` os estados de origem válidos; resultado zero aborta a transação com conflito. Testes cobrem exaustividade, caminhos permitidos, terminais, salto inválido e fence persistido de falha de ingest. Fases sem writer executável não são contadas como integradas; E2E PostgreSQL dos demais writers, versão/artifact, deploy e aceite permanecem pendentes.

Atualização local de ProjectVersion em FR-236 — `current|superseded` é uma projeção da relação com `Project.currentVersionId`, sem coluna de lifecycle mutável. Review v3 combina isso com preview disponível; create v4, duplicate v2, workspace v7, Commands v7/result v6, manual edit v3, patches v3 e LUT read/set v3 usam o mesmo projector. Comparison action v4 projeta current somente para a nova cabeça criada por restore, mantendo accept/reopen com impacto zero e versões preservadas. Todos mantêm contratos anteriores publicados. Testes cobrem histórico, responses correntes e mutations, renderable/deferred, restore, imutabilidade e mismatch. PostgreSQL E2E, transições de artifact, deploy e aceite permanecem pendentes.

Atualização local de lifecycle de artifact em FR-236 — `apollo.artifacts.read` v4 expõe `lifecycleRevision` e o novo comando `apollo.artifacts.lifecycle.transition` aplica a matriz canônica `available↔quarantined→deleted`, com deleted terminal, repetição convergente, motivo obrigatório, idempotência durável e fence anti-ABA no mesmo commit serializável do audit trail. Quarantine/deleted bloqueiam a leitura de bytes já existente; deleted permanece tombstone lógico e não executa descarte físico. Testes controlados cobrem replay, payload divergente, revisão obsoleta, ressurreição rejeitada e no-op sem avanço de revisão. PostgreSQL E2E, deploy e aceite permanecem pendentes.

Atualização local de consumo UI em FR-236 — dashboard de projetos, sala de lotes e editor passaram a usar `visibleState.label/tone/primaryAction/availableActions/terminal` das respostas atuais para badges, contagens, polling e gates de ação. Agrupamentos de navegação continuam locais, mas partem da ação semântica publicada, não de aliases do status técnico. O mapper morto que aceitava `created/analyzing/ready/error/complete` foi removido e testes estruturais impedem sua reintrodução. Faltam E2E visual/browser, deploy e aceite.

| FR-240 | Paridade API-first | S9 | D0,D3,D4,D10 | toda ação da UI, inclusive login/sessão/logout, possui capability e contrato externo sobre o mesmo domínio | contract/e2e |
| FR-241 | Contrato público e descoberta | S9 | D0,D10 | OpenAPI/schemas/versionamento/capabilities publicados | contract/schema |
| FR-242 | Clients, autenticação e escopos | S9 | D0,D7,D10 | sessão humana usa cookie seguro; client revogável usa Bearer e só acessa workspace/scope autorizado; senha nunca vira tool | security/e2e |
| FR-243 | Operações assíncronas | S9 | D4,D10 | job externo acompanha status/result/error/cancel/retry | resilience/contract |
| FR-244 | Webhooks e eventos | S9 | D4,D10 | entrega assinada, deduplicável e recuperável | integration/resilience |
| FR-245 | Idempotência e concorrência externa | S2,S9 | D3,D10 | repetição não duplica e conflito não sobrescreve | property/integration |
| FR-246 | Interface para agentes de IA | S1,S9 | D2,D7,D10 | tools/MCP respeitam schemas, scopes e policies | contract/policy |
| FR-247 | Transferência externa de mídia | S3,S9 | D1,D7,D10 | upload/download resumível sem expor storage interno | security/e2e |
| FR-248 | Preflight e lote externo | S4,S9 | D2,D3,D4,D10 | dry-run prevê impacto/custo e retry é parcial | e2e |
| FR-249 | Governança da API | S7,S9 | D0,D6,D7,D10 | clients/scopes/quotas/webhooks/usage/audit administráveis | security/e2e |

Complemento local F0.100–F0.101/F0.103/FR-249 — todas as capabilities autenticadas cruzam admission ledger PostgreSQL central após auth/policy e antes do handler. Budgets agregados do workspace e budgets do client são avaliados separadamente sob lock do workspace/environment; policies só restringem seus respectivos defaults. Janela móvel, reservas, quota/spend, hash e alertas impedem bypass/race, enquanto 429 e usage-audit v2 não expõem payloads internos. Kill switch global usa o mesmo choke point com recovery humano allowlisted. O E2E HTTP/PostgreSQL foi preparado, mas custo real por client, error-rate agregado, sandbox dos demais providers, execução PostgreSQL, deploy e aceite faltam; FR-249 permanece aberta.

Complemento local seguinte F0.100/FR-249 — list/set/delete de governance policies agora são capabilities públicas `clients:admin`; CAS, confirmação, idempotência ligada ao actor, hashes e command audit transacional impedem overwrite, replay ambíguo e policy cross-workspace/environment. O E2E preparado usa as rotas para todo o lifecycle client-scoped e cria a policy workspace-scoped pela API. Execução PostgreSQL, custo real, anomaly agregado, demais providers sandbox, deploy e aceite faltam; FR-249 permanece aberta.

Complemento local F0.102/FR-249 — capabilities semânticas, ingest/transcrição e stages duráveis de transcript/diarização resolvem provider pelo environment autenticado persistido: sandbox sempre usa fake determinístico e persiste receipt SHA-256 v1/v2, custo simulado e zero external calls; production rejeita fake, exige OpenAI/Groq explícitos e não aceita identidade caller-selected no workflow. Receipts mistos são consultáveis pela capability administrativa v2, redigida e workspace-bound. PostgreSQL/HTTP está preparado mas não executado; providers sintéticos, deploy e aceite faltam, então FR-249 permanece aberta.

Complemento local F0.041/FR-247 — o stream de um download grant deixou de assumir filesystem local e agora segue o driver de artifact storage do runtime. A Application fornece ao port privado key, tamanho e SHA-256 somente depois da autorização; S3 exige checksum, tamanho e `VersionId`, faz `GetObject` dessa versão e confere `Content-Range`, enquanto local verifica hash e invalida cache por mudança de tamanho/mtime. Testes controlados provam full/range, seleção estrutural do driver e bloqueio de adulteração. A jornada HTTP/PostgreSQL/MinIO implantada e o aceite ainda faltam; FR-247 permanece aberta.

Complemento local F0.092/FR-248 — os preflights operacionais de batch edit e variant portfolio materializam `preflight-result/v1` no application service e o publicam como campo obrigatório nos outputs major v2. O fingerprint coincide com o token assinado; conflitos, invalidações, jobs, custo, quota e warnings são derivados do run persistido, inclusive all-or-nothing, skip-failures, no-change, budget e confirmação. Contratos v1 permanecem publicados; PostgreSQL/HTTP implantado e aceite faltam, então FR-248 continua aberta.

Complemento local F0.093–F0.095/FR-248 — token HMAC v1 ganhou serialização canônica, decoder base64url estrito, limites antecipados, claims exatas e compatibilidade regressiva comprovada. A policy central classifica action IDs no servidor e bloqueia classe desconhecida; batch edit commit e confirmação de variant portfolio deixaram de chamar o validador diretamente. Final export unitário foi classificado bounded por formato único aprovado, sem autorizar matriz. Matrix/destructive reais, PostgreSQL/HTTP implantado e aceite faltam; FR-248 permanece aberta.

Complemento local F0.096–F0.097/FR-248 — cada production batch item possui operation ID content-addressed persistido; a página pública bounded e `operations.read/retry` projetam o mesmo aggregate, e retry cria um partial retry real para apenas o step falho sob CAS, lineage, idempotência e escopos `operations:retry` + `projects:write`. Regressões de aplicação substituem a antiga prova puramente funcional para dry-run, expiry e budget, e o E2E PostgreSQL/HTTP foi ampliado para a jornada item page → read → retry. Execução PostgreSQL, deploy e aceite faltam; FR-248 permanece aberta.

Complemento local F0.040/FR-246 — o MCP valida erros REST e resources com os schemas canônicos antes de expô-los, publica metadata de capability/custo/confirmation/trust boundary e rejeita descriptors duplicados ou endpoints não allowlisted. O único target `preflight-token` atual possui binding estrutural explícito: apenas o hash de um token vindo de resposta de preflight validada é retido, e batch/preflight/preflightHash/scopeHash/expiry precisam corresponder antes da chamada; assinatura e vínculos de autoridade continuam na Public API. Testes com client MCP oficial cobrem adulteração, ausência, mismatch e sucesso. PostgreSQL, deploy e aceite permanecem pendentes, portanto FR-246 continua aberta.

Complemento local F0.039/FR-245 — os dez adapters que usam o ledger compartilhado convergem em `readCompletedIdempotencyResponse`: fingerprint diferente produz `IDEMPOTENCY_PAYLOAD_MISMATCH`; status/resposta ausente, não-2xx, JSON não-objeto ou acima de 1 MiB produzem `PERSISTENCE_CONFLICT`. A migration aplica matriz equivalente aos quatro estados e ordena `createdAt <= updatedAt < expiresAt`; replays históricos malformados são removidos sem apagar produto. Testes focados cobrem corrupção, response-loss e scanner exaustivo de consumers. PostgreSQL real, deploy e aceite mantêm F0.039 aberta.

Complemento local de FR-242 — criação inicial e duplicação copy-on-write persistem um `ProjectCreationCommand` imutável junto do Project, ProjectVersion e idempotência. A ação, resultado, origem da cópia e tuple autenticado completo participam do hash; replay por outra credential ou sobre ledger adulterado falha fechado. O seed operacional exige credential/environment explícitos. Testes locais e jornadas PostgreSQL foram preparados, mas execução hospedada, deploy, aceite e cobertura das demais mutações ainda mantêm F0.036 aberta.

Complemento local de FR-242 — annotation, proposta individual e lote de review patch preservam a credential ou sessão humana iniciadora desde a rota até o aggregate PostgreSQL. O context hash cerca fingerprint, replay e vencedor concorrente; a autoria pública é derivada e vinculada ao client Bearer ou usuário delegado, sem expor o tuple privado. Hidratação e constraints falham fechadas para grupo parcial, adulteração ou projeção de autor divergente. Testes locais e jornadas PostgreSQL foram preparados; execução hospedada, deploy e aceite ainda mantêm F0.036 aberta.

Atualização local de FR-243 — `PublicOperation.projectId` tornou-se a associação canônica e indexada para operações de projeto, enquanto `artifact-render` permanece global. As capabilities de operação evoluíram para v9 e aceitam o novo tipo `project-director-run`, cujo alvo/result é uma `project-version` reservada; os demais tipos continuam vinculados a `media-artifact`, com falha fechada no domínio, banco e reidratação. `POST /v1/projects/{projectId}/director-runs` responde 202 e persiste contexto privado, base/hash, delegação e idempotência. O worker usa claim, heartbeat, retry e fence de lease+attempt; seu commit serializável publica versão, snapshots, Command, DirectorRun, invalidações, outbox e sucesso da operação atomicamente. Testes locais cobrem domínio, enqueue/replay, worker/fence, contratos e uma jornada PostgreSQL/API preparada; execução hospedada, deploy e aceite permanecem pendentes, portanto F0.037 continua aberta.

Atualização local de FR-244 — um único builder de domínio minimizado deriva eventos de mudanças de status de `PublicOperation`; o adapter persiste o lote no mesmo transaction client da mutação. Criação, claim, wait/resume, retry, cancelamento, sucesso, falha e expiração geram `operation.status.changed`, com o evento terminal adicional apenas para sucesso/falha. Criações especializadas de long-form/source-cleanup, conclusão do Diretor e cancelamento em massa por controle de acesso participam da mesma garantia. CAS perdido, replay e mudança apenas de fase não duplicam evento. Testes locais cobrem domínio e doubles transacionais; jornadas PostgreSQL foram ampliadas para outbox, atomicidade, replay e redaction, mas ainda não foram executadas neste host. Deploy e aceite permanecem pendentes; F0.038 continua aberta.

Complemento local de FR-244 — o mapeamento comum de `PublicEvent` para o outbox foi extraído para impedir divergência entre writers. Review annotation cria evento minimizado no mesmo commit da annotation; apply unitário e batch publicam uma resolução por annotation somente após vencerem os fences. Lifecycle publica `artifact.ready` ao restaurar availability e `artifact.rejected` ao entrar em quarantine, sem fingir evento para delete/no-op. Suspensão explícita de client publica `client.suspended` junto do cancelamento cercado das operações; reason nunca entra no payload. Testes locais provam artifact replay/no-op, client containment, redaction e contagem; E2Es PostgreSQL foram preparados para criação/resolução e rollback. `budget.threshold.reached`, execução hospedada, deploy e aceite permanecem abertos.

Complemento local F0.038/FR-244 + F0.025/FR-128 — `production-batch-budget-thresholds/v1` deriva cruzamentos de 80% (`warning`) e 100% (`exhausted`) exclusivamente do custo real acumulado nos steps persistidos. O repositório grava os eventos minimizados no mesmo transaction client da ação vencedora; reserva, preflight, start/cancel/resume, cache hit e replay não publicam. Uma ação que atravessa ambos registra os dois fatos em ordem canônica. Testes de domínio cobrem limites, monotonicidade, vínculo de workspace e redaction; o E2E PostgreSQL foi preparado para atomicidade e replay, mas não executado neste host. Os 14/14 tipos possuem producer local; execução hospedada, deploy e aceite mantêm F0.038 aberta.

Complemento local de administração de FR-244 — `WebhookControlRoom` entra pela sessão humana existente e usa exclusivamente 15 ações `/v1` já publicadas para catálogo, endpoints, subscriptions, deliveries, attempts, challenge, lifecycle revisionado, rotação e replay. O relatório determinístico `ui-capability-parity-report/v1` prova zero ação sem capability e zero rota fora de Application service. Testes estruturais cobrem carga paralela, fences/idempotência, attempts e segredo one-shot sem persistência no browser. E2E HTTP/PostgreSQL com browser, operações terminais, deploy e aceite permanecem pendentes.

Atualização local de FR-243 — contratos genéricos de operação v10 e long-form v3 adicionam custo público sem inferência. A estimativa de `long-form-index` é recalculada do workflow canônico persistido; o custo real é exposto apenas em `succeeded`, `failed` ou `canceled` e confere com a medição persistida. Reidratação falha fechada em JSON/hash/colunas divergentes, operações sem fonte confiável omitem custo e retry remove a medição terminal anterior. Testes locais de domínio, aplicação, presenter e contratos passaram; PostgreSQL/HTTP hospedado, outros tipos, deploy e aceite continuam pendentes.

Atualização local de fase/progresso em FR-243 — artifact render não salta mais de `rendering` para `persisting`: o application boundary entra em `verifying` antes da segunda materialização/revalidação e só entra em `persisting` após direitos e inputs permanecerem idênticos. As quatro fases expõem progresso determinístico 0/4→4/4. Testes cobrem render novo, recuperação de output comprometido, perda de lease e cancelamento durante verification sem promoção stale. Métricas internas do encode e cobertura equivalente dos demais tipos continuam abertas.

Complemento local de integridade em FR-243 — os sete tipos executáveis possuem sequência, denominador e unidade fechados no domínio. `queued`, cada fase `running`, checkpoints interrompidos e sucesso têm relações canônicas distintas; hidratação recusa ausência, troca de unidade, total incorreto ou fase/contador divergentes. A migration converte apenas filas antigas 0/1 reconhecíveis e substitui a constraint permissiva pela mesma matriz fail-closed. Testes percorrem todos os tipos e adulterações de domínio/adapter; o E2E PostgreSQL foi preparado para provar a constraint, ainda sem execução isolada. Provider/sync/batch, deploy e aceite permanecem abertos.

Atualização local de resiliência em FR-243 — o worker do Diretor cobre restart após indisponibilidade transitória, reclaim de lease expirada com incremento de attempt, rejeição do commit stale, cancelamento durante planejamento sem nova versão e esgotamento terminal em dead-letter. A prova de esgotamento corrigiu um defeito: a última tentativa não pode carregar `nextAttemptAt`. A jornada PostgreSQL permanece preparada, não executada neste slice; providers/sync/batch continuam pendentes.

## F1 — MVP Core

| Req | Título | Spec | Dep. | Evidência de aceite | Teste |
|---|---|---|---|---|---|
| FR-002 | Dashboard de projetos | S7 | D0,D4,D6 | cards/progresso refletem jobs reais | e2e |
| FR-003 | Busca e filtros | S7 | D0,D6 | filtros combinados retornam projetos | integration |
| FR-004 | Ações rápidas | S7 | D0,D3,D6 | abrir/revisar/duplicar/arquivar funcionam | e2e |
| FR-010 | Objetivo estratégico | S1 | D0,D2,D6 | rubrica correta entra no DirectorRun | integration |
| FR-011 | Ação desejada | S1 | D0,D2 | CTA/destino persistidos e validados | policy |
| FR-012 | Briefing livre opcional | S1,S7 | D0,D6 | projeto segue com campo vazio | e2e |
| FR-013 | Brief Compiler | S1 | D2,D7 | prompt vira estrutura/conflicts | unit/golden |
| FR-014 | Modo media-only | S1 | D2 | tratamento é inferido sem freeform | e2e |
| FR-021 | Override por projeto | S7 | D0,D7 | inherit/none/custom resolvem por elemento | integration |
| FR-030 | Tipos de entrada | S3 | D1,D6 | vídeo/áudio/imagem entram no ingest | e2e |
| FR-040 | Media Library | S3 | D0,D1,D6 | assets indexados e navegáveis | e2e |
| FR-042 | MediaSegment | S3 | D0,D1 | range reutiliza master | integration |
| FR-047 | Image Library | S3 | D1,D6 | OCR/descrição/busca/reuse | e2e |
| FR-049 | Catalogação automática | S3 | D1,D4 | asset aprovado entra no índice | integration |
| FR-050 | PerceptionTimeline | S1,S3 | D1,D2 | timeline agrega sinais mínimos | golden |
| FR-051 | EditorialBeat | S1 | D2 | beats independem de subtitle chunk | unit |
| FR-052 | Confidence | S1 | D2 | decisões carregam confidence/evidence | contract |
| FR-060 | TreatmentPlan | S1 | D2 | objetivo produz gramática/energy policy | golden |
| FR-061 | StoryPlan | S1 | D2 | atos/blocos/dependências persistidos | golden |
| FR-062 | Alternativas de montagem | S1 | D2 | ao menos candidatos elegíveis são comparados | integration |
| FR-063 | Segurança narrativa | S1 | D2,D7 | qualifier/causalidade não são removidos | policy/golden |
| FR-064 | Ferramentas do Diretor | S1 | D2,D3 | Director só altera via tools | integration |
| FR-065 | Decisions log | S1 | D0,D2 | razão/evidência/custo persistidos | integration |
| FR-066 | Budget | S1 | D2,D4 | limite encerra geração com estado válido | resilience |
| FR-090 | Talking head | S1,S2 | D1,D2,D3,D5 | raw talking head gera final | e2e |
| FR-091 | Visual montage / voiceover | S1,S2 | D1,D2,D3,D5 | áudio+B-roll sem pessoas | e2e |
| FR-160 | Formatos obrigatórios | S2,S7 | D3,D5,D6 | contrato aceita 5 ratios | schema |
| FR-163 | Responsive placement | S2,S7 | D3,D5 | anchors/constraints adaptam canvas | visual golden |
| FR-164 | Reframe | S2,S7 | D2,D3,D5 | face/object permanece visível | visual golden |
| FR-165 | Crítica por formato | S1,S7 | D2,D5 | issue é específico do output | integration |
| FR-170 | Estilos iniciais | S7 | D3,D5 | 5 presets renderizam | visual golden |
| FR-171 | Modos | S7 | D0,D3,D6 | auto/default/manual/none | e2e |
| FR-172 | SubtitleStylePreset | S2,S7 | D3,D5 | schema e responsive overrides | unit/golden |
| FR-173 | Anchor por percepção | S1,S7 | D2,D3 | legenda evita rosto/elemento | visual golden |
| FR-174 | Override por segmento | S2,S7 | D3 | override não muda global | integration |
| FR-175 | Sidecar | S2 | D3,D5 | SRT/VTT seguem alignment | fixture |
| FR-210 | Preview interativo | S7 | D5,D6 | pause/seek frame-accurate | e2e |
| FR-211 | ReviewAnnotation | S7 | D0,D3,D6 | annotation persiste contexto | integration |
| FR-212 | Escopos | S7 | D3,D6 | current/all formats/locales | unit/e2e |
| FR-213 | RenderElementMap | S7 | D3,D5 | hit-test seleciona layer correta | visual/e2e |
| FR-214 | Patch automático | S1,S7 | D2,D3,D6 | annotation vira proposta gated, command e versão imutável | T-FR-214 unit/API/Postgres/render/visual |
| FR-215 | Batch review | S7 | D3,D6 | 2–100 propostas compilam PatchSet único; conflito atômico não muda versão; partial-retry é explícito | T-FR-215 domínio + Postgres serializável/rollback + API HTTP + UI |
| FR-216 | Edição manual | S2,S7 | D3,D6 | timeline deriva do EditPlan corrente; trim/split/move/replace/crop normalizado/inspector persistem como Command + snapshot + versão; crop é visível na UI e consumido pelo renderer; undo/redo criam versões filhas; API e mouse/teclado usam o mesmo service | T-FR-216/T-FR-233 domínio + contratos v2 + PostgreSQL preparado + API HTTP + UI + golden FFmpeg real |
| FR-217 | Compare | S2,S7 | D3,D5,D6 | toggle/split/overlay usam snapshots e proxies version-bound; playhead só sincroniza com mapping compartilhado; duração, score, issues e diff semântico são explícitos; accept/reopen persistem Command e restore cria child version sem apagar A/B | T-FR-217 domínio + PostgreSQL serializável/idempotência/conflito + API HTTP nos três modos + Chromium com versões de durações diferentes |
| FR-230 | Proxy first | S1,S7 | D3,D4,D5,D6 | worker persiste proxy H.264 + laudo version-bound; hard bloqueia, warning exige decisão CAS append-only; final lê somente `ready-for-final`; UI/API exibem o mesmo estado e medem upload→proxy real | T-FR-230 domínio + worker + PostgreSQL limpo/49 migrations + API Bearer/idempotência/conflito + Chromium/sessão |
| FR-231 | Final render | S2 | D3,D4,D5 | output aprovado gera artifact | e2e |

## F2 — Lotes, reuso e formatos

| Req | Título | Spec | Dep. | Evidência de aceite | Teste |
|---|---|---|---|---|---|
| FR-043 | SpeechSegment | S3 | D1,D2 | frase/range/alignment pesquisáveis; API pública cataloga e busca segmentos virtuais com metadados/proveniência sem criar mídia | T-FR-043 5/5 + PostgreSQL/API E2E 1/1 + produção `fbbd4c5`, run `speech-catalog-run-0a57512a-e85a-4cd6-9bc4-6f32821f310c` |
| FR-044 | EvidenceSegment | S3 | D1,D7 | claim/qualifier/consent preservados | policy |
| FR-045 | LongFormMoment | S3 | D1,D2 | chapter/moment indexados | integration |
| FR-046 | ValidatedSegment | S3 | D1,D7 | validationScope/protected envelope | integration |
| FR-048 | Busca híbrida | S3 | D1,D2 | filtros+OCR+vector+rerank | retrieval eval |
| FR-053 | Processamento hierárquico | S1,S3 | D1,D2,D4 | long-form não exige visão integral | T-FR-053 + API/PostgreSQL E2E + smoke de produção |
| FR-080 | ProductionBatch | S4 | D0,D1,D4 | lote e items independentes | T-FR-080 + API/PostgreSQL E2E + E2E visual e smoke de produção |
| FR-081 | Script alignment | S4 | D1,D2 | roteiro↔fala↔range com confidence | golden |
| FR-082 | Biblioteca de takes | S3,S4 | D1,D2 | alternates classificados | integration |
| FR-083 | Compatibility graph | S4 | D2,D7 | hard failures e scores explicáveis | unit/golden |
| FR-084 | VariantRecipe | S4 | D0,D2,D3 | H+B+C com lineage | integration |
| FR-085 | Anti-explosão combinatória | S4 | D2,D4 | preflight/top-N/budget | e2e |
| FR-086 | Edição em lote | S4,S7 | D3,D6 | scope/impacto explícitos | e2e |
| FR-087 | Partial retry | S4 | D4 | item falho retenta isolado | resilience |
| FR-120 | Source Deconstruction | S3 | D1,D2 | clean range, report, contexto preservado e comparação source/clean | T-FR-120 domínio/Golden Reel + API/PostgreSQL E2E + UI desktop/mobile + produção `d2bb805` |
| FR-121 | Contaminação | S3 | D1,D2 | cinco tipos localizados por range/região/confidence, impacto de remoção e diagnóstico Director/humano | T-FR-121 + seis fixtures audiovisuais + API/PostgreSQL E2E + UI + produção `e00727f` |
| FR-122 | Limpeza MVP | S3 | D1,D5 | trim/reframe/cover/reject | visual golden |
| FR-124 | Validation envelope | S3 | D1,D7 | copy/take/framing/timing/opening derivados do escopo; proteção automática; saída com aprovação e log preserved/lost; hook exato + corpo/prova/CTA sem excesso | T-FR-124 domínio + API/PostgreSQL E2E + constraints + UI `/v1` |
| FR-130 | Proof need | S1,S3 | D2 | StoryPlan declara claim/tipo/função/momento; busca EvidenceSegments autorizados primeiro; seleciona evidência exata ou registra `proof-unavailable`/`no-proof-needed`; card genérico é impossível | T-FR-130 golden + round-trip canônico + API/PostgreSQL E2E + constraints + UI `/v1` |
| FR-131 | Integrity gate | S1,S3 | D2,D7 | claim/produto/pessoa/período/audience/consent/rights/context comparados contra recipe e EvidenceSegment exatos; prova incompatível, expirada ou descontextualizada bloqueada; attribution/qualifiers idênticos no visual e verbal; nenhuma fabricação | T-FR-131 policy eval 14 casos + round-trip/tamper + API/PostgreSQL E2E + constraints + UI `/v1` |
| FR-132 | Modos de prova | S1,S2 | D2,D3,D5 | cada ProofIntegrity aprovado gera matriz segmento×formato com cutaway/split/card, claim exata, timing/transição/layout seguro, attribution/qualifiers exatos e override hash-scoped; prova/contexto incompatível falha fechado | T-FR-132 domínio + compiler/RenderInput + 15 visual goldens revisados + 3 MP4 Remotion inspecionados; API/PostgreSQL E2E e deploy ainda pendentes |
| FR-133 | Long-form indexing | S3 | D1,D2,D4 | workflow probe→transcript→diarization→chunks→moments com checkpoints, referência tipada ao resultado persistido, parcial pesquisável, budget e retomada sem duplicação | T-FR-133 state machine 6 casos + application/API contract 4 casos + worker orchestration 5 casos + transcript 3 casos + diarização 9 casos focados e 1 integração FFmpeg local + derived stages 7 casos + fencing Prisma controlado 4 casos + E2E API/worker/PostgreSQL de 2h com restart 1 caso. Schema/migration/repositório/API pública e orquestração durável com lease fencing estão implementados localmente. O E2E cria banco limpo com `pgvector`, aplica todas as migrations, parte sem transcript, persiste transcript e diarização controlados, interrompe o primeiro worker em chunks e comprova por `GET /v1` a operação `retrying`, transcript pesquisável e os cinco tiers/status explícitos; depois fecha o Prisma, retoma em uma nova instância e gera 24 chunks e moments pesquisáveis dentro do budget sem duplicar transcript, diarização ou índices. A operação conclui e o postflight confirma zero conexões órfãs. Transcript ausente prepara áudio do artifact imutável, reutiliza somente provider/model/adapter exatos sem novo custo e persiste `V2MediaTranscript`; diarização persiste clusters anônimos; chunks/moments preservam lineage. O daemon supervisionado executa o router integral, propaga shutdown ao provider/FFmpeg, desconecta o Prisma e já possui container de deploy com verificação de restart. Execução E2E com providers reais, deploy e aceite permanecem pendentes; por isso F2.022 continua aberto |
| FR-134 | Contiguous extraction | S1,S3 | D1,D2 | janela autocontida duração-alvo | T-FR-134 domínio da extração 5 casos + domínio da evidência 3 casos + domínio do sidecar de transcript 2 casos + application de extração 3 casos + application do produtor de evidência 3 casos + application do produtor de avaliação 3 casos + analisadores de rights/transcript 4 casos + avaliador determinístico 3 casos + adapter/analisador de áudio 2 casos, sendo 1 FFmpeg real + adapter/analisador visual 2 casos, sendo 1 FFmpeg real + integração/replay no estágio moments 3 casos + adapters Prisma controlados 12 casos + contrato público 2 casos: contrato `contiguous-extraction/v1` seleciona deterministicamente uma única faixa semântica autorizada, pontua autocontenção/densidade/integridade/áudio/visual somente com evidence refs, compila StoryPlan e EditPlan de um clip sem síntese multi-range ou zoom automático e vincula hash à lineage. Cada evidência canônica vincula o index run e moment exatos, faixa, fatos ordenados e provider/model/version/input/output do analisador; o produtor exige uma observação por moment, faz replay antes do analisador e permite runs separados por tipo para não fabricar cobertura ausente. O estágio `moments` persiste atomicamente um sidecar que liga cada moment aos spans exatos, hierarchical run e transcript; hashes de texto/span e o conjunto declarado na aggregation são recalculados dentro da transação fenced. Os analisadores determinísticos de boundary e density derivam começo/fim alinhados, gaps, pontuação terminal, palavras por minuto e cobertura de fala somente desse sidecar; ausência ou adulteração falha fechado. O adapter de evidência hidrata e revalida o sidecar antes de expô-lo aos analisadores. O analisador de rights/consent deriva integridade do snapshot corrente e falha fechado em bloqueio ou cancelamento. O adapter FFmpeg de áudio mede loudness EBU R128, true peak, volume e silêncio em cada faixa recomendada somente após verificar tamanho e SHA-256 dos bytes imutáveis, e revalida o checksum após todas as medições; a localização do storage não participa do hash editorial portátil. O adapter FFmpeg visual mede luminância, saturação, diferença temporal, outliers, repetição, pixels fora do broadcast range, black/freeze e mudanças de cena por faixa, incluindo intervalos de freeze ainda abertos no fim da janela, sob a mesma verificação de bytes imutáveis. O avaliador `apollo/contiguous-evidence-policy/1.0.0` exige exatamente uma evidência de cada uma das cinco dimensões, deriva scores somente dos fatos canônicos e aplica tags de objetivo determinísticas; ausência, silêncio, black frame ou integridade incompleta rejeitam o moment em vez de fabricar aprovação. O estágio executa os cinco produtores e o avaliador após persistir o índice; operação, attempt, lease não expirada, input hash e idempotency do checkpoint são revalidados dentro das transações. Se a lease cair depois do índice ou durante a avaliação, o retry reutiliza o índice e retoma evidências/avaliação sem duplicá-lo. O hash da avaliação também vincula provider/model/version e hashes exatos do input/output; a API expõe essa provenance, mas não aceita candidates nem scores. O produtor de avaliação exige uma decisão avaliada ou rejeitada para cada moment, recusa referências fora da allowlist dimensional e faz replay antes de chamar o provider. O adapter Prisma desse produtor relê source, evidence, rights, ator, operação e checkpoint dentro de transação serializable, invalida somente avaliações ativas substituídas e persiste run/decisions/evaluations atomicamente sob a lease de `moments`. Esses adapters foram comprovados apenas com clients controlados, não em PostgreSQL real. O application service público recebe apenas objetivo/tópico/duração, lê candidates e scores pela porta confiável, persiste uma extração e converge idempotência sem reavaliar. O adapter de extração filtra índice ativo e rights/consent correntes, recalcula `evaluationHash`, persiste JSON canônico e faz FK do resultado para moment, index run e avaliação selecionados. A API local `POST /v1/projects/{projectId}/contiguous-extractions` e `GET /v1/projects/{projectId}/contiguous-extractions/{extractionId}` expõe os mesmos services com auth/scope, idempotência, schemas, exemplos e capability IDs. O roteiro E2E local combinado agora prepara 24 janelas semânticas de 120 s em um master controlado de 7.200 s, exige cinco evidências e uma avaliação por moment, cria/lê/reexecuta a extração via `/v1` e verifica plano single-range e persistência única; ele recusa host não local, exige `application_name`/limites de pool/timeouts, desconecta ambos os clients e consulta `pg_stat_activity` no postflight. Essa extensão compila, mas ainda não foi executada por ausência de PostgreSQL local descartável. O schema anterior foi aplicado do zero em PostgreSQL/pgvector local (110 tabelas/566 índices/435 FKs); as migrations aditivas passam validação estrutural (114/591/455), mas uma nova aplicação do zero está pendente porque este host não possui PostgreSQL/Docker/Podman/WSL local e o incidente bloqueia uso da VPS. O comportamento permanece apenas parcialmente integrado; E2E PostgreSQL/API, golden real de 2h, UI, deploy e aceite estão pendentes |
| FR-136 | Repositório semântico | S3 | D1,D2 | consulta cross-asset reutiliza índice | retrieval eval |
| FR-180 | ColorPipeline | S2,S3 | D1,D5 | technical→match→LUT→output | visual golden |
| FR-181 | Workspace LUT Library | S3,S7 | D0,D1,D6 | upload/select/disable .cube | e2e |
| FR-182 | ColorPlan | S2 | D3,D5 | global/source/segment override | integration |
| FR-235 | Export matrix | S2,S4 | D3,D4,D5 | variants×formats preflight/render | e2e |

Atualização local de FR-134 — a superfície do editor possui 1 contrato estático adicional: busca momentos exclusivamente por `GET /v1/projects/{projectId}/long-form-moments`, envia a `POST /v1/projects/{projectId}/contiguous-extractions` somente objetivo, tópico normalizado, duração, tolerância e FPS com chave de idempotência estável, e apresenta uma faixa, cinco dimensões de evidência, o avaliador, um clip e zoom automático desligado. O fluxo foi inspecionado em desktop e mobile com respostas `/v1` controladas; essa inspeção comprova composição e responsividade da UI, não integração com backend/PostgreSQL. O E2E com PostgreSQL descartável, o deploy e o aceite continuam pendentes; FR-134 permanece parcial e nenhuma caixa de produto foi concluída por esta atualização.

Golden local de FR-134 — `T-FR-134` materializa com FFmpeg um master MP4 real de 7.200 s, distingue por pixels uma janela semântica dourada de 120 s do restante escuro, calcula o SHA-256 dos bytes, escolhe a faixa 59:00–61:00 entre dois moments avaliados e entrega o `EditPlan` V2 ao renderer novo. O teste comprova MP4 de 120 s, 960×540, áudio AAC, uma fonte, um clip, ausência de síntese multi-range e zoom automático desligado; amostras antes/depois do range são escuras e início/meio/fim da saída preservam a faixa dourada. Essa evidência é local e determinística, não substitui o master real da Imersão nem o E2E API/PostgreSQL.

Atualização local de FR-136 — a consulta pública existente aceita `scope: project|workspace`, preserva `project` como padrão e usa o projeto da rota como âncora de autorização. No novo scope de workspace, o adapter consulta somente documentos do mesmo workspace, hidrata o snapshot corrente de rights/consent e elimina candidatos sem permissão para o uso solicitado antes do rerank; `includeBlocked: true` e filtro `rights: blocked` falham fechados nesse scope. A capability usada pelo Diretor recebe intenção, atmosfera, pessoas, fala e visual em canais estruturados: atmosfera e pessoa são restrições exatas; fala precisa corresponder ao transcript; visual precisa corresponder a OCR, descrição ou metadata; esses canais participam da recuperação full-text/vetorial sem aceitar candidates ou scores do chamador. A command capability idempotente `semantic-search.reuse-runs.create` reexecuta a query exata, confere `queryHash` e o `resultSetHash` content-addressed exibido ao Diretor, registra o audit confiável de até 500 candidates (incluindo bloqueios de rights/consent anteriores ao rerank), mede latência e exige que todos os resultados elegíveis sejam particionados entre reutilizados e rejeitados com motivo enumerado; o run e sua decisão são imutáveis no Postgres V2. A capability aditiva `semantic-search.scale-evaluations.create` mantém o contrato `retrieval-evaluation/v1` intacto, fixa escopo e quantidade real de documentos ativos, exige de 3 a 50 julgamentos, mede cada busca com relógio monotônico, agrega precision/recall/nDCG/MRR e latência mínima/p50/p95/máxima/média, rejeita qualquer mudança do corpus durante o run e revalida o tamanho dentro da transação serializável antes de persistir `retrieval-scale-evaluation/v1`. Testes controlados medem snapshots imutáveis com 10, 100 e 1.000 documentos e comprovam qualidade, percentis, hash e fail-closed por drift; testes de application/contrato também comprovam propagação do scope, instante único de avaliação, retorno cross-project no mesmo workspace, default restrito ao projeto, separação entre evidência falada/visual, partição obrigatória, recusa de result set stale e distinção entre rejeição de política e rejeição editorial. O E2E isolado prepara dois projetos do mesmo workspace e exercita consulta, reuse run e scale evaluation pela API/PostgreSQL, incluindo replay, tamanho cross-project e auditoria de um asset bloqueado, mas não foi executado neste host sem PostgreSQL local descartável. Execução PostgreSQL real, série crescente em banco real, deploy e aceite continuam pendentes; F2.024 permanece aberto.

Atualização local de FR-180 — o contrato `color-plan/v1` materializa color space, transfer, primaries, matrix, range e bit depth na entrada, na saída e em cada estágio `technical → match → creative-lut → output`. Todos os quatro estágios precisam estar explícitos; a resolução ordena o pipeline independentemente da ordem de declaração, permite override source/camera/segment somente por substituição, recusa estágio duplicado dentro do mesmo layer, exige continuidade colorimétrica entre estágios, vincula provider/version/parâmetros e seu SHA-256 e só permite LUT em `creative-lut` ativa com artifact ID/hash imutáveis. O resultado `resolved-color-pipeline/v1` possui manifesto e hash canônico. O ingest V2 agora solicita ao `ffprobe` color space, transfer, primaries, range, pixel format e bit depth tanto do master quanto do proxy, deriva HDR SDR/HLG/PQ deterministicamente, identifica a versão e o SHA-256 do executável real e persiste as duas evidências `media-color-probe/v1` de forma imutável na mesma transação que conclui a operação. Metadata incompleta fica explicitamente `unavailable`, sem default silencioso. A capability pública autenticada `GET /v1/artifacts/{artifactId}/color-probe` lê o mesmo registro sob escopo de workspace. As capabilities `POST /v1/projects/{projectId}/color-pipeline-compilations` e `GET /v1/projects/{projectId}/color-pipeline-compilations/{compilationId}` compilam e persistem um pipeline imutável apenas para source artifact/manifest realmente associado ao projeto. O request não aceita `sourceMetadata` nem input de estágio: o application service lê o probe exato do Postgres e encadeia cada input a partir dessa evidência; probe indisponível falha fechado. A tabela V2 projeta o hash do probe, pipeline, compilation e cada transform/provider/version/parametersHash, com idempotência por ator, replay convergente e reidratação que detecta tamper. O adapter `FfmpegColorPipelineProcessor` valida novamente os hashes, exige providers e parâmetros allowlisted, materializa LUT somente por caminho absoluto previamente resolvido, executa exatamente `zscale technical → match → lut3d/none → zscale output`, grava metadata explícita e reprova divergência no probe da saída. O golden real gera três MP4 SDR distintos (`testsrc2`, `smptebars` e rampa limitada), produz três hashes distintos, confirma Rec.709/8-bit e preserva pelo menos 40 níveis na rampa entre preto e branco, cobrindo clipping por inspeção de pixels. Enqueue de proxy e export final agora exige exatamente uma compilação para cada fonte de vídeo, grava `artifactId/manifestId/compilationId/compilationHash/pipelineHash` no contexto imutável e no input hash, e recusa ausência, ambiguidade ou drift. Os workers reidratam e revalidam os hashes antes de chamar o renderer; o renderer pré-processa cada fonte pelo pipeline FFmpeg e só então compõe clips, legendas e áudio. A seleção também entra na recipe de proxy/final e no `RenderInput` final. O golden real multi-source comprovou master + B-roll, preservação do áudio do master, frame visual do B-roll e canvas/fps final. O E2E API/PostgreSQL foi ampliado para rota, replay e registro persistido quando houver PostgreSQL descartável. O ColorPlan editável e seus overrides pertencem a F2.027; ainda faltam executar o E2E PostgreSQL neste host, ligar a seleção editável futura à ProjectVersion, implantar e obter aceite visual do MP4 real de recuperação. F2.025 permanece aberto e nenhuma caixa foi marcada.

Atualização local de FR-181 — o agregado imutável `workspace-lut-version/v1` valida `.cube` 3D de até 8 MiB, tamanho 2–65, domínio finito, quantidade exata de amostras e conteúdo canônico; nomes e `TITLE` Unicode são preservados. A versão vincula workspace, owner, licença e política de uso, tags, compatibilidade de cor, intensidade padrão, hashes do conteúdo/registro e preview PNG. O Postgres V2 possui head lógico ativo/inativo e versões imutáveis, com idempotência por ator, ponteiro de versão corrente, integridade referencial e reidratação que detecta adulteração do registro ou dos bytes do preview. As capabilities públicas autenticadas permitem importar, listar, ler e obter o preview por `/v1/workspaces/{workspaceId}/luts`; o import gera de fato um PNG 512×288 com FFmpeg `lut3d`, persiste os bytes e faz replay sem gerar novamente. A criação de versões sucessoras exige `baseVersion` corrente, recompila o `.cube`, gera preview novo e só avança o ponteiro em transação serializável com sequência contígua; versões históricas possuem leitura e preview próprios. O lifecycle ativo/inativo tem revisão monotônica, comando imutável por ator, idempotência e compare-and-swap; desativar é remoção lógica e não apaga versões/previews antigos. O default do workspace também é um agregado versionado: começa explicitamente sem configuração na revisão zero, aceita somente a versão corrente de uma LUT ativa ou `none`, persiste cada revisão imutável e avança o head por CAS serializável; a API pública lê e altera esse mesmo estado. A seleção por projeto aceita `workspace-default`, uma versão imutável explícita ou `none`; ela resolve o default para a identidade exata da LUT no momento do comando, persiste `set-project-lut-selection` no modelo comum de `EditCommand`, cria uma nova `ProjectVersion` sem mutar snapshots anteriores e avança projeto/head por CAS serializável. As capabilities `GET/POST /v1/projects/{projectId}/lut-selection` usam o mesmo application service e devolvem a seleção resolvida sem expor estado interno. Os workers de proxy e export final agora localizam a seleção efetiva na ancestry da ProjectVersion, recusam pipeline criativo divergente, derivam deterministicamente o `.cube` pela intensidade selecionada, gravam-no em diretório efêmero do operation ID e entregam ao renderer somente o path absoluto materializado; cleanup ocorre em `finally`, inclusive após cancelamento ou perda de lease. O export final registra seleção, hash do cubo derivado e um asset LUT portátil no `RenderInput`, sem persistir paths locais. A autorização e a reconstrução genéricas reconhecem esse asset tipado: a identidade lógica codifica LUT/version/intensidade/hash, a versão histórica é relida do Postgres mesmo após desativação do head, os bytes são derivados novamente em cache local content-addressed sob a raiz privada e hash/tamanho são conferidos antes da lease chegar ao renderer. A decisão de uso persiste uma identidade de política genérica sem forjar FK de mídia; `owned`/`licensed` são permitidas e `restricted` falha fechado, com revalidação da identidade imutável no worker. Testes locais cobrem LUT válido com glyph/nome incomum, linhas/domínio/diretivas inválidos, contrato exato, replay, base stale, licença/tags, lifecycle/default, seleção por projeto e `none`, materialização com intensidade, identidade portátil adulterada, política restrita, cache contido, cleanup e FFmpeg real usando somente a LUT pré-materializada. O E2E API/PostgreSQL foi ampliado para cobrir versão resolvida, Command, ProjectVersion, ancestry, replay, leitura, stale base e `none`, mas não foi executado porque este host não possui PostgreSQL descartável e a VPS permanece bloqueada pelo incidente operacional. Ainda faltam storage S3-compatible/signed URL para reconstrução distribuída, UI comparativa, execução do E2E PostgreSQL, deploy e aceite visual. F2.026 permanece aberto e nenhuma caixa foi marcada.

Atualização local de FR-233 — o fluxo `manual-edit` produz `command-impact/v1` content-addressed com identidades do Command/versão-base/versão-resultado, change/dependency types, ranges frame-first, formato configurado, outputs proxy/final concluídos da base e o proxy mínimo esperado. O mapa entra no payload schema v2 do mesmo `V2EditCommand`, participa do outbox por hash/counts e é reidratado com validação estrutural, consistência e hash. O adapter Prisma descobre apenas operations com `status=succeeded` e `phase=completed` da versão-base e relê o conjunto dentro do commit serializável; drift aborta sem escrita parcial. Na mesma transação, cada output afetado ganha uma relação `command-artifact-invalidation/v1` content-addressed, com FKs para Command, base/result version e artifact, status `stale`, variant, dependências, ranges e `impactHash`; outra variant e seleção sem semântica de render não geram linha, e o `status` global dos bytes históricos permanece `available`. Replay compara as linhas normalizadas ao payload e falha em divergência. Os schemas públicos `command-impact/v1`, `command-artifact-invalidation/v1` e `project-artifact-invalidations/v1` foram adicionados; a capability GET aditiva `apollo.projects.artifact-invalidations.read` consulta a versão atual ou uma versão-resultado explícita. O worker V2 deriva do Command persistido um range proxy, valida hash/tamanho/duração do proxy concluído da versão-base, recompõe somente o trecho e monta um MP4 completo reutilizando prefixo/sufixo; manifest e lineage registram a fonte reutilizada. A conclusão grava `V2CommandArtifactInvalidationResolution` imutável com operação/artifact/manifest substitutos, e a leitura deixa de expor o stale somente após `succeeded`. Seleção com impacto zero produz cache hit terminal sobre o proxy-base, sem novo artifact, cor ou worker, com revalidação e FKs. Crop manual agora é operação tipada com retângulo normalizado: persiste no clip/snapshot/ProjectVersion, aparece na timeline e UI, gera dependência apenas visual, restringe artifact/variant/range e é aplicado pelo FFmpeg antes da composição. O golden real comprova crop azul somente no range central entre prefixo/sufixo vermelhos, duração integral e `RenderElementMap` coerente. A edição `inspect.text` materializa o cue sobreposto e deriva o stale dos frames exatos do único cue alterado; outro golden FFmpeg comprova pixels diferentes apenas no intervalo central da legenda, prefixo/sufixo idênticos, duração integral e presença temporal coerente no `RenderElementMap`. O caso B-roll agora parte do `replace` materializado no EditPlan, preserva `audioSourceArtifactId` e os frames do master, usa range/hash do impacto real e comprova fonte visual nova somente no centro do MP4, vizinhos reutilizados, áudio AAC e duração integral. `replace-source-transcript` adiciona a troca API-first de evidência imutável: valida ID/hash/source, retima palavras no áudio corrente, persiste snapshot/ProjectVersion/Command/outbox e relações stale para todos os outputs concluídos da base, sem enfileirar proxy; a resposta exige `run-director`, e o repositório do Diretor lê o transcript explicitamente selecionado no EditPlan mesmo que exista linha posterior. As capabilities evoluíram com majors compatíveis e baseline deliberadamente atualizado. Testes de domínio passam e o E2E PostgreSQL/API cobre replay, persistência, ausência de enqueue e seleção exata do Diretor, mas não foi executado neste host. Ainda faltam outros Command types, múltiplos ranges/rates, E2E PostgreSQL, deploy e aceite. F0.027 permanece aberto e nenhuma caixa foi marcada.

Complemento local de FR-233 — `remove-spoken-content` agora persiste `editorial-cut-impact/v1` com identidades do Command/base/resultado/transcript, full-timeline seguro, dependências de áudio/conteúdo/timing/visual, variants, outputs proxy/final concluídos e um proxy integral mínimo do formato corrente. O adapter Prisma relê outputs no commit serializável, grava e reidrata `command-artifact-invalidation/v1`, rejeita hash/linhas divergentes e não fabrica stale quando a base não possui output concluído. A rota `/v1/projects/{projectId}/commands` enfileira o proxy pelo mesmo application service V2 do Diretor, com chave derivada idempotente, e devolve a operação pública junto do impacto e das invalidações. Testes locais cobrem outputs existentes/ausentes, adulteração e replay; os E2Es PostgreSQL/API foram ampliados para persistência, enqueue e replay, mas permanecem não executados neste host. Isso não conclui F0.027: outros Commands, múltiplos ranges/rates, deploy e aceite continuam abertos.

Complemento local de FR-233 — `run-director` agora persiste `director-run-impact/v1` no payload v2 do mesmo Command que referencia as snapshots de percepção, TreatmentPlan, StoryPlan, EditPlan e critic. O mapa vincula transcript/planner/critic, cobre o maior timeline base/resultado, declara áudio/conteúdo/policy/timing/visual, lista apenas outputs proxy/final concluídos da base e exige exatamente um proxy integral do formato corrente. O adapter Prisma relê o conjunto na transação serializável, grava e reidrata `command-artifact-invalidation/v1`, rejeita drift/tamper e não cria stale sem output-base. A resposta pública v5 expõe impacto, invalidations e operação; o E2E PostgreSQL/API preparado cobre output-base concluído, persistência, enqueue e replay, mas não foi executado neste host. Demais Commands, múltiplos ranges/rates, deploy e aceite continuam abertos; F0.027 não está concluído.

Complemento local de FR-233/FR-181 — `set-project-lut-selection` agora persiste `project-lut-selection-impact/v1` no payload schema v2 do `V2EditCommand`, vinculado à seleção e à identidade/hash da LUT resolvida. Com timeline renderizável, o impacto declara full-timeline e dependência visual, lista somente proxies/finais concluídos da versão-base, grava relações `command-artifact-invalidation/v1` no mesmo commit serializável e enfileira um proxy integral da versão-resultado pelo application service V2 comum. Sem output-base não fabrica stale. Antes do ingest, a seleção permanece versionada, mas `renderDeferredUntilTimeline=true` exige ranges, variants, artifacts, invalidations e renders mínimos vazios e a rota não cria operação. As capabilities de leitura/escrita evoluíram para v2 com schemas e exemplos para ambos os estados. Testes locais passam e os E2Es API/PostgreSQL foram preparados para deferimento, persistência, full impact, enqueue e replay, mas não foram executados neste host; deploy e aceite continuam abertos, portanto nenhuma caixa foi marcada.

Complemento local de FR-233 — o executor de proxy parcial agora consome até oito ranges canônicos disjuntos do `command-impact/v1`, recompõe cada range e intercala os resultados com todos os segmentos válidos do proxy-base. O domínio ordena e funde overlap/adjacência; repository e renderer recusam forma divergente, limite excedido ou cobertura integral e usam render completo como fallback seguro. Clips usam rate frame-first em `[0.25, 4]`, `setpts` no vídeo e cadeia `atempo` no áudio; reverse e timing não demonstrável falham fechados. O mapeamento parcial arredonda ambos os boundaries absolutos, evitando o frame extra possível ao arredondar offset e comprimento separadamente. Goldens FFmpeg locais comprovam dois ranges disjuntos com miolo reutilizado frame a frame, rate 2 e 0,5 com contagem exata, áudio AAC, crop, legenda e B-roll sem regressão. E2E PostgreSQL multi-range, rate em `replace-source-transcript`, deploy e aceite permanecem abertos; F0.027 não está concluído.

Complemento local de FR-233 — a cobertura de política por Command ganhou um registro canônico e fechou o último tipo sem impacto. `src/v2/domain/edit-command-registry.ts` declara os oito Command types persistíveis com render policy, schema de impacto, obrigatoriedade, tolerância a `renderSemanticsChanged=false`, motivo de deferimento e a evidência em código de cada classificação; `createEditCommand` recusa tipo não registrado e o repositório de proxy deriva a allowlist de reuso parcial do registro. `compare-action` passou a persistir `compare-action-impact/v1` content-addressed no payload schema v2 do mesmo `V2EditCommand`: `resultVersionId` é o próprio `baseVersionId` (versão preservada), `changeKinds=[review-state]`, e dependências, ranges, variants, artifacts e renders mínimos ficam vazios sob `renderSemanticsChanged=false`. Nenhuma `command-artifact-invalidation/v1` é gravada e nenhum render é enfileirado; o evento `project.status.changed` declara `commandImpactHash` e `artifactInvalidationCount=0`. A hidratação Prisma exige schemaVersion 2, reparseia o impacto e confere identidade/hash contra a linha persistida; payload legado sem impacto falha fechado como `PERSISTENCE_CONFLICT`, decisão consciente porque o produto é pré-produção e o reset destrutivo está autorizado. A capability `apollo.projects.version-comparisons.act` já estava em 2.0.0, então a evolução aditiva foi para 3.0.0 com `project-version-comparison-action-result/v3` incluindo `impact`, mantendo request/v1 e result/v2 publicados; a baseline pública foi regenerada deliberadamente e o único contrato alterado é essa capability mais o schema aditivo. Evidência local: `tests/v2/compare-action-impact.test.mjs` (fábrica, parser, adulteração, serviço com fakes que provam zero enqueue e zero invalidation, validação Ajv do contrato v3) e `tests/v2/edit-command-registry.test.mjs`. O E2E PostgreSQL de `tests/v2/prisma-manual-edit.integration.mjs` foi ampliado com accept, replay, conflito de revisão com rollback, reopen, payload/hash persistidos, evento de outbox e contagens inalteradas de versões, invalidations e operações de render, mas não foi executado: este host não tem PostgreSQL descartável nem Docker, e a VPS não é ambiente E2E. Continuam pendentes execução PostgreSQL, integração em `main`, deploy e aceite; F0.027 permanece aberto e nenhuma caixa foi marcada.

Correção auditada de FR-233 — multi-range é uma capacidade do executor, não permissão para declarar falsamente o miolo como reutilizável. O primeiro teste integrado proposto movia um clip de `[0,30)` para `[150,180)` e tratava `[30,150)` como válido, embora os clips intermediários fossem retimados. `move`, trim e demais mudanças downstream agora mantêm um envelope contínuo desde o menor frame afetado até o fim. Ranges disjuntos só podem ser produzidos por Commands que demonstrem mapping idêntico nos intervalos preservados. A regressão usa o materializador manual real com duas tracks e impede reintrodução da subinvalidação.

Complemento local de FR-233 — `replace-source-transcript` agora usa o mesmo módulo de timing frame-first do renderer para clips com rate `[0.25, 4]`. O mapeamento valida timing de áudio/vídeo e limites da timeline, percorre clips audíveis na ordem da timeline, duplica deterministicamente a evidência quando o mesmo source aparece em mais de uma posição e descarta palavras parcialmente cobertas. Testes calculados à mão cobrem rate 2 e 0,5, B-roll com áudio do master, source reordenado, source repetido, compressão a um frame e falhas fechadas. Um golden local atravessa replacement → snapshot persistível → Diretor → FFmpeg real e comprova 240 frames, rates `1`, `2` e `0.5`, áudio AAC, evidência visual comprimida/expandida, legendas retimadas e ausência nos gaps descartados. Ainda faltam E2E PostgreSQL/API, deploy e aceite; F0.027 permanece aberto.

Atualização local de FR-234 — o composition root do render aceita agora `local` ou `s3` explicitamente. No modo S3-compatible, mídia continua vinculada ao artifact workspace-scoped do Postgres; o worker executa `HeadObject` autenticado com checksum mode, exige tamanho, SHA-256 full-object e `versionId` exatos e só então assina um GET SigV4 da versão imutável. O máximo configurável é 30–300 segundos, mas o TTL efetivo é encurtado imediatamente antes da assinatura para nunca ultrapassar a autorização; autorização vencida impede até o `HeadObject`. Checksum multipart composto não é tratado como hash do objeto inteiro; o upload precisa preservar SHA-256 full-object nativo ou em metadata `apollo-sha256`. A URL existe somente no `MaterializedRenderInput` capturado pela lease e não aparece em receipt, output ou API. HTTP é aceito apenas para endpoint loopback de desenvolvimento; fora disso, somente HTTPS. Testes controlados usam o SDK AWS real contra servidor HTTP local para provar assinatura de `HeadObject`, presigned GET versionado, expiração limitada pela autorização, checksum/tamanho, falha sem versionamento e rechecagem do Postgres antes do storage. O smoke Remotion autorizado também consumiu o MP4 por URL HTTP versionada, promoveu o segundo MP4 e comprovou que receipt não contém parâmetros `X-Amz-*`. MinIO/S3 real, bucket policy/versioning/lifecycle, upload S3, fonts/data, E2E PostgreSQL, deploy e aceite continuam pendentes; F0.028 permanece aberto e nenhuma caixa foi marcada.

Complemento local de FR-234 — `font` e `data` deixaram de ser kinds apenas declarativos do RenderInput: são `MediaArtifactType` persistível, entram em manifests content-addressed, passam por disponibilidade workspace-scoped e rights revalidation e são materializados pelos adapters local e S3 com identidade/tamanho/checksum exatos. O adapter local mantém contenção por realpath e leitura estável; o S3 exige a mesma versão imutável e URL de lease já usada por mídia. LUT continua fora do media lookup e usa seu repository versionado. A capability `apollo.artifacts.read` evoluiu para v2, preservando v1, para representar os novos tipos. Testes locais usam bytes reais de fonte/dado e o cliente S3 controlado; MinIO/S3 e PostgreSQL reais, render que consuma esses recursos, deploy e aceite permanecem pendentes.

Complemento local de FR-234 — o compiler da composição Apollo resolve `fontAssetId` e `renderDataAssetId` somente contra assets materializados do kind exato. Dados entram por `apollo-video-render-data/v1`, com propriedades fechadas e texto limitado; o adapter relê bytes `file:` ou HTTP(S) sem redirect, com timeout e limite de 64 KiB, e compara tamanho/SHA-256 antes de interpretar UTF-8 JSON. A composição usa família interna fixa e bloqueia o frame até `FontFace.load()`. Um golden Remotion real produziu MP4s cujo frame decodificado muda ao remover a fonte ou os dados e recusou adulteração de bytes e URI inválida. A evidência é local; PostgreSQL/MinIO reais, deploy e aceite permanecem pendentes.

Correção local de FR-234 — `media-artifact-manifest/v4` deixou de aceitar RenderInput com recurso não-LUT oculto do lineage. A criação exige igualdade ordenada por canonical key, SHA-256 e role entre `manifest.sources` e todos os assets de vídeo, áudio, imagem, fonte ou dado; a validação inversa também impede source declarada que não participa do render. Preflight, autorização e revalidação da lease repetem o gate depois de autenticar o protected RenderInput, portanto registros anteriores assimétricos falham antes de availability, rights, resolução de localização ou render. LUT permanece excluída somente porque sua identidade e rights são resolvidas pelo repositório versionado especializado. As regressões cobrem fonte omitida, lineage persistido adulterado e LUT legítima; PostgreSQL real, golden reexecutado, deploy e aceite permanecem pendentes.

Complemento local de FR-234 — o golden de reconstrução serializa em disco `media-artifact-manifest/v4`, payload canônico de `render-input/v1`, rights e identidades dos assets e, depois dessa fronteira, reidrata somente a fixture salva. Duas autorizações independentes passam pelos services reais de autorização, materialização verificada e render Remotion. Os dois MP4s mantêm 270×480, 30 fps, duração tolerante à cauda AAC e hashes `framemd5` idênticos para toda a sequência decodificada de vídeo e áudio. PostgreSQL/MinIO reais, deploy e aceite continuam pendentes; a caixa permanece aberta.

Evidência integrada F0.030/FR-234 — o run `30763463127` promove master e derivados com SHA-256 full-object e `VersionId` obrigatório, apaga o namespace local antes do proxy, relê ProjectVersion/EditPlan, artifacts e manifests do PostgreSQL, rematerializa a versão exata do MinIO em storage efêmero pertencente à operação e executa FFmpeg real. O proxy promovido é reaberto por outro work root e validado por bytes, checksum, 540×960 e duração. O mesmo run mantém API e quatro workers supervisionados sem restart e executa teardown integral. A exceção HTTP aceita somente loopback ou service name Compose de rótulo único com opt-in explícito; hostname público/dotted continua exigindo HTTPS. Isso fecha a microtarefa de reconstrução do proxy, mas não conclui toda F0.028/FR-234, deploy ou aceite.

Evidência integrada F0.030/NFR-003 — o run `30765019248` aplicou em PostgreSQL limpo as tabelas tipadas de eventos e alertas operacionais, comprovou gravação idempotente por hash, isolamento por workspace e agregados exatos de fila, execução, span, bytes, tokens e custo. A capability pública `GET /v1/operations/telemetry/summary` exige `operations:read`, limita a consulta a 31 dias e devolve somente contagens/somas/máximos sem trace, job, project ou payload do provider. O mesmo run passou 807 testes, contratos, build, integrações API/PostgreSQL, goldens e Compose supervisionado com cleanup. Dashboard por jornada/fase, hard-invariant alerts, implantação e aceite operacional permanecem abertos; NFR-003 e a caixa de F0.030 não estão concluídos.

Evidência integrada F0.031/FR-242 — o ADR-142 seleciona OIDC Authorization Code + PKCE, sessão opaca PostgreSQL com idle/absoluta/rotação/revogação e recuperação exclusivamente no IdP. O registry recusa qualquer `apollo.sessions.*` com `toolName`; catálogo de agente, MCP e Diretor não recebem capabilities humanas. Os runs `30766068302`, `30768229810`, `30768936087` e `30770605092` comprovaram revogação/throttle, proteção SSR, membership e troca isolada de workspace. O run `30771147749` tornou o cookie integralmente opaco. O run `30773060731` executou IdP mock + Next produção + PostgreSQL e verificou discovery, Authorization Code, S256 PKCE, state, browser binding, nonce, JWKS, issuer, audience e assinatura. O run `30773970385` passou 823 testes, build, schema de 137 tabelas/671 índices/516 FKs, goldens e Compose; a jornada PostgreSQL/HTTP comprovou rotação HMAC convergente aos 10 minutos, predecessor bloqueado nas APIs comuns, mesmo sucessor para requisições concorrentes, recovery de 60 segundos, limite rígido de 15 minutos e zero token bruto persistido. As três microtarefas de shell/switch/E2E permanecem abertas no percentual auditado porque IdP real, recuperação, implantação e aceite ainda não ocorreram.

## F3 — Sintético e transformação

| Req | Título | Spec | Dep. | Evidência de aceite | Teste |
|---|---|---|---|---|---|
| FR-092 | Synthetic presenter | S6 | D1,D4,D5,D7 | personagem IA+B-roll final | e2e |
| FR-093 | Hybrid | S1,S2,S6 | D1,D2,D3,D5 | real+sintético+prova coexistem | e2e |
| FR-100 | Audio-first | S6 | D1,D3 | áudio governa alignment/timeline | integration |
| FR-101 | Adapters | S6 | D4 | provider mock substituível | contract |
| FR-102 | Geração por blocos | S6 | D2,D4 | retry/reuse por block | integration |
| FR-103 | SyntheticPresenterProfile | S6 | D0,D7 | profiles/version/consent | policy |
| FR-104 | SyntheticMasterAsset | S3,S6 | D1,D4 | bruto+áudio+config salvos | integration |
| FR-105 | Cache | S6 | D1,D4,D7 | hash reutiliza artifact válido | integration |
| FR-106 | Crítico sintético | S1,S6 | D2,D4 | lips/identity/pronunciation gates | eval |
| FR-110 | TransformationBrief | S1,S6 | D2,D4 | intent/preserve/fallback estruturados | contract |
| FR-111 | Modos | S6 | D4 | 6 modos no capability registry | contract |
| FR-112 | Provider Registry | S6 | D4 | routing por capability/custo | integration |
| FR-113 | Jobs duráveis | S6 | D4 | API/MCP resume | resilience |
| FR-114 | Novelty budget | S1 | D2 | excesso bloqueado/penalizado | unit |
| FR-115 | Fallback | S6 | D2,D4 | v2v→composite→cutaway | integration |
| FR-116 | Crítico | S1,S6 | D2,D4 | transform rejeitada com issue | eval |
| FR-123 | Limpeza avançada | S3,S6 | D1,D4 | separation/inpaint como derivado | visual eval |
| FR-218 | Mask future | S6,S7 | D3,D4,D6 | annotation region vira mask input | integration |

## F4 — Multicâmera e long-form avançado

| Req | Título | Spec | Dep. | Evidência de aceite | Teste |
|---|---|---|---|---|---|
| FR-135 | Editorial synthesis | S1,S3 | D1,D2,D3 | multi-range preserva contexto | golden/policy |
| FR-140 | CaptureSession | S5 | D0,D1,D8 | tracks agrupados | integration |
| FR-141 | Session clock | S5 | D8 | mapping canônico | property |
| FR-142 | Estratégias de sync | S5 | D8 | cascade seleciona método | fixture |
| FR-143 | TrackCoverage | S5 | D8 | fontes curtas/gaps representados | property |
| FR-144 | Drift | S5 | D8 | anchors corrigem rate | numeric fixture |
| FR-145 | Piecewise maps | S5 | D8 | stop/rewind/seek mapeados | property |
| FR-146 | Sync audio separado | S5 | D1,D8 | scratch descartável | integration |
| FR-147 | Capture Protocol | S5,S7 | D6,D8 | requisitos exibidos/salvos | e2e |
| FR-148 | Apollo Sync Marker | S5 | D1,D8 | flash+chirp detectados | fixture/e2e |
| FR-149 | SyncDiagnostic | S5,S7 | D6,D8 | confidence/coverage/warnings | e2e |
| FR-150 | Direção multicâmera | S1,S5 | D2,D3,D8 | ângulo por speaker/contexto | golden |
| FR-183 | Multicam match | S2,S5 | D1,D5,D8 | câmeras equilibradas antes da LUT | visual eval |
| FR-184 | Crítico de cor | S1,S2 | D2,D5 | clipping/skin/mismatch localizados | visual eval |

## F5 — Localização e áudio

| Req | Título | Spec | Dep. | Evidência de aceite | Teste |
|---|---|---|---|---|---|
| FR-094 | Music-led montage | S8 | D3,D5,D9 | cuts seguem grid sem deformar fala | audiovisual golden |
| FR-190 | Conteúdo canônico | S8 | D0,D2,D9 | ScriptBlocks sourceLocale | schema |
| FR-191 | LocalizationVariant | S8 | D0,D3,D9 | locale possui assets/plano/status | integration |
| FR-192 | Timings próprios | S8 | D3,D9 | alignment novo recompila timeline | property |
| FR-193 | Modos de áudio | S6,S8 | D4,D7,D9 | TTS/local/upload autorizado | integration |
| FR-194 | LocaleProfile | S8 | D0,D7,D9 | glossary/CTA/font/RTL | integration |
| FR-195 | Assets localizáveis | S3,S8 | D1,D2,D9 | OCR decide share/localize/reject | eval |
| FR-196 | Crítico de localização | S1,S8 | D2,D9 | fidelity/pronunciation/lips/subtitle | eval |
| FR-200 | Sync modes | S8 | D3,D9 | narrative/music/hybrid persistidos | unit |
| FR-201 | AudioDirectionPlan | S8 | D3,D9 | beat/sections/events/mix compilam | golden |
| FR-202 | Sound Library | S3,S8 | D1,D9 | BPM/rights/tags pesquisáveis | integration |
| FR-203 | Sound budget | S1,S8 | D2,D9 | repetição/densidade limitadas | unit |
| FR-204 | Mix/master | S8 | D5,D9 | ducking/loudness/limiter | audio fixture |
| FR-205 | Crítico audiovisual | S1,S8 | D2,D5,D9 | masking/drift/tails/issues | audiovisual eval |

Complemento local de FR-233/FR-214 — `apply-review-patch` persiste `command-impact/v1` no payload v2 do mesmo Command, converte ranges humanos em frames pelo FPS do EditPlan, transforma ponto em um frame e usa envelope downstream para `trim`/`move`. O adapter relê outputs proxy/final concluídos no commit serializável, grava `command-artifact-invalidation/v1`, reidrata payload/hash/linhas e aborta em drift. O repository de proxy reconhece o Command e materializa range reuse somente contra o artifact-base listado pelas invalidations. A capability `apollo.projects.review-patches.apply` evoluiu para v2 e expõe impacto e stale relationships. Um golden FFmpeg real parte do PatchSet aplicado e comprova que somente o range frame-first da legenda muda pixels, preservando prefixo/sufixo do proxy-base. Contrato e testes locais passam, enquanto o E2E PostgreSQL/API preparado, deploy e aceite continuam abertos.

Complemento local de FR-233/FR-215 — `apply-review-patch-batch` persiste o mesmo `command-impact/v1` no payload v2, combina a classificação de até cem operações e canonicaliza seus ranges frame-first sem transformar mudanças locais disjuntas em envelope contínuo; operações de timing continuam invalidando do menor range/target afetado até o fim. O escopo de variants reúne o formato corrente e os `formatIds` das annotations incluídas. O adapter Prisma descobre outputs concluídos, relê e fenceia o conjunto no commit serializável, grava relações stale, e o replay valida identidade, hash e linhas normalizadas. A capability de aplicação do lote evoluiu para v2 e o repository de proxy aceita esse Command para range reuse. Testes locais passam e o E2E PostgreSQL/API foi ampliado para persistência e replay, mas permanece não executado neste host; deploy e aceite continuam abertos.

Correção operacional de FR-233 — tanto a promoção de proxy quanto a de final resolvem somente relações stale da versão-resultado, kind e variant exatos. A leitura considera a resolução apenas depois que a operação relacionada está `succeeded`. Para tolerar perda de lease entre promoção e settlement, o adapter atualiza por upsert a identidade `(invalidationId, operationId)`: um retry do mesmo operation ID substitui artifact/manifest anteriores antes de poder tornar a resolução efetiva. Isso impede que bytes de uma tentativa órfã sejam apresentados como substituto válido quando outra tentativa conclui. A prova local usa adapters Prisma controlados; PostgreSQL real e deploy permanecem pendentes.

Evidência integrada F0.032 — o run hospedado `30774673461` tornou a direção das dependências um gate executável para Domain, Application, Infrastructure, Public API, Agent, MCP e UI. O scanner cobre também Web/Editor e rejeita acesso direto a Prisma gerado, repository factory, storage e adapters fora de composition roots nomeados. O próprio gate revelou e removeu Application→Infrastructure nos workers de ingest/cleanup e Agent→MCP no Diretor; T-F0.032 prova violações representativas e atravessa sinteticamente web→application→domain→orchestrator→provider→critic→compiler→renderer. A matriz completa de PostgreSQL, MinIO, FFmpeg, Remotion, API/OIDC e Compose ficou verde. A prova ainda é estrutural e parcial: não substitui E2E de todos os componentes, isolamento operacional completo, deploy ou aceite, portanto nenhuma caixa de F0.032 foi fechada.

Complemento integrado F0.032 — o run `30775230222` adicionou ports tipados com referências versionadas entre Provider Registry/perception, Director, Critic, Compiler e Renderer. O orchestrator valida contexto/outputs, propaga cancelamento, exige hash canônico do RenderInput e não chama Compiler/Renderer após reprovação. T-F0.032 atravessa a cadeia completa com fakes e cobre cancelamento e input não canônico antes do provider. Os 826 testes e toda a matriz hospedada passaram; conexão ao pipeline produtivo, deploy e aceite permanecem abertos.

Evidência integrada F0.033 — o run `30775662600` transformou as 57 entidades normativas do PRD 10.1–10.6 em inventário verificável: 26 targets de tabela, 5 snapshot kinds, 14 value objects e 12 lacunas `planned`. T-F0.033 deriva o conjunto do PRD, exige unicidade/owner/lifecycle, valida models Prisma, símbolos exportados e kinds e proíbe tabela conceitual genérica. A Spec 10 inclui o diagrama de ownership. O gate não afirma que as 12 lacunas foram implementadas nem substitui teste do grafo completo de FKs, deploy ou aceite; as caixas permanecem abertas.

Complemento integrado F0.033 — o run `30776437393` validou os seams centrais das Specs 02/03/06: manifest de SourceAsset sem URI pública, TimelineSegment frame-first, OutputSpec, EditCommand e um novo port AsyncMediaProviderAdapter completo. O boundary de capabilities é fechado, imutável e TTL-bound e rejeita campos extras/secrets, duplicatas, operações desconhecidas e ranges inválidos. Os 832 testes, build, PostgreSQL/MinIO, goldens, API/OIDC e Compose passaram. DocumentAsset e ProviderJob/provider real durável continuam pendentes; a caixa permanece aberta.

Evidência concluída F0.033 — o run `30776962496` integrou a fonte única `config/platform-versions.json` e o gate `platform:validate`, que cruza manifests, lockfiles, imagens Postgres/pgvector/MinIO e ADR-001/002/008. O drift do Next foi corrigido para 16.2.12 e o registry parcial foi removido. FFmpeg/ffprobe 8.1.1 são alvos explícitos; probes e goldens continuam comprovando a versão/comportamento executados. Audits root/Remotion ficaram em zero vulnerabilidades e a matriz completa passou. A microtarefa de fixação de versões está concluída.

Evidência concluída F0.033 — as fontes oficiais verificadas em 2 de agosto de 2026 classificam Next.js 16.x como Active LTS e React 19.2 como a linha atual, com o patch 19.2.7 publicado. O upgrade controlado `c350181` aplicou o codemod de params assíncronos; a baseline corrente usa Next 16.2.12/React 19.2.7. O primeiro run de fechamento revelou um `503` na disputa de rotação de sessão OIDC; `c491c9c` tornou esse conflito recuperável por retry limitado e comprovou oito refreshes PostgreSQL concorrentes convergindo. No run `30778010953`, `next typegen` e TypeScript validaram os dynamic routes, os 189 contratos `/v1` e a integração OIDC permaneceram íntegros, Remotion foi empacotado e renderizado com Chromium real e o build App Router passou junto à matriz completa. A microtarefa de atualização sem quebra está concluída.

Evidência concluída F0.033 — o run `30778629569` aprovou audits root/Remotion com zero vulnerabilidades após instalação limpa. O gate de plataforma exige `uuid` 11.1.1, `postcss` 8.5.18 nos dois locks e no override root, e ausência de `form-data` nas duas árvores; assim, a correção não depende somente da disponibilidade temporal do advisory no registry. A microtarefa dos três advisories não-major está concluída.

Evidência concluída F0.033 — no run `30807744785`, o runtime selecionou S3 explicitamente e atravessou PostgreSQL + MinIO versionado: promoveu bytes imutáveis, removeu o namespace local, rematerializou por key/checksum/tamanho/VersionId, renderizou, reabriu e validou o proxy e executou teardown integral. O boundary de render ainda limita signed URL pela autorização e relê identidade workspace-scoped antes do storage. A configuração S3-compatible está concluída.

Evidência concluída F0.033 — `createV2PostgresClient` recusa ausência, `file:` e protocolos não-PostgreSQL, e o schema V2 declara somente PostgreSQL. O commit `3aba0f9` adicionou gate contra imports e dependências SQLite e contra alteração do datasource, com testes que falsificam cada caso. O run `30807744785` aprovou 832 testes, migrations limpas e a matriz API-first. A microtarefa PostgreSQL-only está concluída.

Evidência concluída F0.033 — a Spec 10 passou a documentar o grafo relacional executável dos aggregates centrais, e T-F0.033 verifica relações, campos compostos, targets únicos, delete policy, alcance a Workspace e falsificações cross-project/cross-artifact diretamente contra Prisma. A migration `20260803130000_central_reference_graph` torna ProjectVersion, snapshots, DirectorRun e os inputs materializados de ingest/render/review/export fail-closed no PostgreSQL. O run inicial `30809633413` revelou que IDs de output de ingest são reservas anteriores à materialização; a correção preserva essas reservas como escalares e exige artifact+manifest no registro terminal. O run final `30809793462` aprovou schema limpo com 137 tabelas, 677 índices e 525 FKs, 833 testes, vertical smoke, reconstrução PostgreSQL/MinIO, build, Remotion, integrações Prisma/API e teardown completo. A microtarefa de documentação e integridade do grafo central está concluída; as 12 entidades `planned` continuam visíveis nas duas primeiras microtarefas de F0.033.

Evidência concluída F0.034/FR-240 — `capability-registry.ts` registra 189 capabilities imutáveis com metadados runtime validados. O scanner TypeScript cobre 73 call sites atuais da UI, resolve 66 capabilities/endpoints e atravessa cada rota até os mesmos Application services consumidos por clientes externos. Seis superfícies internas possuem allowlist tipada, justificativa e chaves proibidas nos schemas públicos; não existe exceção por texto livre. O relatório determinístico `docs/quality/ui-capability-parity-report.json` relaciona UI × capability × endpoint × services × tests e cobre 189/189 contratos, sem ações órfãs, rotas divergentes ou capabilities injustificadas. Os runs `30811367434`, `30811899810`, `30812369531` e `30812924567` aprovaram incrementalmente os gates; o último passou 837 testes, baseline pública, build, PostgreSQL/MinIO, media/Remotion, integrações e teardown. F0.034 está integralmente concluída como gate estrutural integrado; implantação/aceite das capabilities de produto continuam pertencendo aos respectivos requisitos funcionais.

Evidência concluída F0.035/FR-241 (convenções e source of truth) — o contrato machine-readable centraliza `/v1`, JSON, ID, UTC, frames, paginação e filtros e é consumido por presenters/schemas. A autenticação externa valida query names, repetição e parâmetros obrigatórios contra a capability somente após autenticar; URLs assinadas fazem o mesmo explicitamente. O gate AST prova correspondência exata entre 189 handlers, 189 capabilities e seus query params; schemas e OpenAPI continuam derivados do mesmo registry, com 339 schemas e 151 paths. A primeira execução hospedada passou o contrato base (`30814516664`); `30815108018` falsificou uma ambiguidade literal/dinâmica real, corrigida por precedência de template em `776558a`. O run `30815677386` aprovou 841 testes, baseline com somente a evolução aditiva do MVP Core, build, PostgreSQL/MinIO, API/OIDC, media/Remotion e teardown. Esses dois primeiros itens foram encerrados nesse run; os complementos seguintes encerram erros, examples/documentação, detector, depreciação e cobertura individual.

Evidência concluída F0.035/FR-241 (erros, documentação e evolução) — o catálogo fail-closed cobre 117 códigos, sete categorias, status/retry e mensagens públicas redigidas; envelope v3 preserva v1/v2 e inclui o fallback 500. HTTP/PostgreSQL comprovou auth e policy sem detalhes internos. Os 385 examples dos 340 schemas são validados e empacotados no prebuild em 342 arquivos content-addressed; CI publicou o bundle do SHA. O detector distingue compatibilidade de baseline tracking: adição continua não-breaking, mas precisa de update revisado para ficar protegida contra remoção futura. Ele recusou `error-envelope/v3` ausente antes da baseline receber somente esse schema. Os runs `30819557419`, `30820090576` e `30820627232` passaram build, artifact upload, PostgreSQL/MinIO, API/OIDC, media/Remotion e teardown. A evidência seguinte encerra depreciação/sunset e contract test individual por operação.

Evidência concluída F0.035/FR-241 (depreciação e cobertura exaustiva) — o registry imutável de depreciações exige refs/paths canônicos, datas UTC e janela mínima de 180 dias. `error-envelope/v1` emite Structured Field Date, HTTP-date e guide v1→v3; o v3 não emite aviso. O primeiro HTTP run revelou que o proxy autenticado ocultava o guide estático e falhou, e `ece1bab` passou a liberar somente paths presentes no registry. O bundle agora inclui o guide e 343 arquivos com manifest. T-FR-241 executa 189 subtests independentes sobre handler, boundary, OpenAPI, auth, parâmetros, status, media types, schemas/examples e envelope v3. O run `30823898229` aprovou 1.035 testes, hash documental `2d29b3a0...`, PostgreSQL/MinIO, HTTP/OIDC, build, mídia/Remotion e teardown. F0.035 está integralmente concluída; isso não antecipa deploy ou aceite das capabilities funcionais.

Evidência concluída F0.036/FR-242 (modelo de identidade externa) — `ApiClient` v2 separa identidade, tipo, grants canônicos, ambientes autorizados e criador; `ServiceAccount` e `ApiCredentialRef` são value contracts explícitos sem secret. A migration `20260803150000_api_client_identity_model` substitui `environment/scopesJson` por `allowedEnvironmentsJson/scopeGrantsJson`, adiciona type/createdBy e remove os campos anteriores, sem dual-read. Autenticação Bearer e sessão humana consultam somente o modelo novo. As responses v2 de list/create/rotate expõem a identidade canônica e preservam v1 apenas como contrato publicado. O run `30827500404` aprovou 1.036 testes, clean migration, Postgres/MinIO, contracts/paridade, HTTP/Prisma, build, goldens e teardown. Esta evidência encerra somente a primeira microtarefa de F0.036; emissão, secret reference/rotation, deny-by-default, audit, kill switches e security E2E permanecem abertos.

Evidência concluída F0.036/FR-242 (emissão e validação Bearer) — a credencial opaca usa prefixo versionado, IDs validados e secret aleatório de 256 bits; somente salt de 128 bits e hash `scrypt` de 32 bytes são persistíveis. Os parâmetros atuais `N=16384/r=8/p=1` e limites de memória estão pinados, `timingSafeEqual` faz a comparação e parser/header/material persistido inválido falham fechado sem diagnóstico diferenciador. T-FR-242 fixa o vetor derivado e falsifica formatos, tamanhos, IDs, salt/hash e headers ambíguos ou excessivos. O run `30829124000` repetiu 1.036 testes e toda a matriz PostgreSQL/HTTP, migrations, contracts/paridade, build, mídia e teardown. Esta evidência encerra somente a segunda microtarefa de F0.036; as cinco seguintes permanecem abertas.

Evidência concluída F0.036/FR-242 (one-shot, referência, rotação e revogação) — `ApiCredentialRef` identifica a credential independente; `ApiClient` não possui verifier e o secret bruto nunca é persistido. A migration `20260803190000_contract_api_credential_verifiers` remove as cópias salt/hash da identidade e limita o verifier one-way ao credential owner. Create/rotate retornam token somente ao primeiro vencedor; replay, perda de response e concorrência devolvem IDs sem token. A janela idempotente usa o clock do Application service, overlap é limitado e revoke concorrente converge antes de o token passar a 401. O run `30830871011` aprovou 1.037 testes, migration limpa, API/PostgreSQL, contratos/paridade, mídia/build e teardown. Esta evidência encerra a terceira microtarefa; deny-by-default, audit context, kill switches e security E2E permanecem abertos.

Evidência local F0.036/FR-242 (matriz deny-by-default) — `API_SCOPE_MATRIX` fecha os 13 grants operáveis e é reutilizada por criação/hidratação de client, autenticação, `requireScope` e validação do registry. T-FR-242 prova igualdade matriz↔capabilities, recusa `projects:delete` apesar do formato válido e resolve cada endpoint até a rota/Application service que aplica seu guard. A prova ainda não atravessou PostgreSQL/HTTP hospedado nem deploy/aceite, portanto a microtarefa permanece aberta no status auditado.

Evidência local F0.036/FR-242 (audit binding) — os dois autenticadores derivam `ExternalAuditContext` da identidade persistida e `requireScope` falha antes da autorização se client, credential, workspace, environment ou delegação divergir do actor. A sessão humana vincula member/identity/role como um conjunto completo; Bearer não recebe esses campos. O conjunto de grants autenticados é uma view imutável sem métodos de mutação. Testes provam binding, falsificação cross-workspace, delegação parcial e ausência de `add`; ledger PostgreSQL completo, execução hospedada, deploy e aceite permanecem pendentes.

Incremento local F0.036/FR-242 (persistência de audit sensível) — `ApiAccessCommand` mantém sua projeção pública redigida, mas o repositório persiste credential/session identity, environment, authentication kind, member, login identity, role e `actorContextHash` canônico. O Application service inclui o hash no fingerprint e o repositório recalcula o tuple hidratado; outro member usando o mesmo client UI não pode recuperar o replay. Como comandos anteriores não possuem atribuição reconstruível, a migration contratual os trunca antes de tornar os campos obrigatórios, coerente com o reset V2 pré-produção autorizado. Prova PostgreSQL hospedada, extensão do ledger às demais capabilities, deploy e aceite permanecem abertas.

Incremento local F0.036/FR-242 (administração de identidade) — `V2ApiAdministrationCommand` registra create/rotate/revoke com target client+credential, audit tuple completo, context hash, fingerprint e instante. Create/rotate gravam command junto de identity, verifier one-way e idempotency response; o hash do actor entra no fingerprint. Revoke passou de update convergente isolado para transação serializável que muda a credential e cria um único command terminal. Replays administrativos e credentials já revogadas antes do contrato são removidos pela migration pré-produção em vez de receber autoria fabricada. Prova hospedada e cobertura das demais famílias ainda pendem.

Incremento local F0.036/FR-242 + F0.038/FR-244 (administração de webhook) — `V2WebhookAdministrationCommand` registra as onze mutações de endpoint/subscription/signing secret/delivery/event, agora incluindo challenge, com target, endpoint/estado/base quando aplicável, fingerprint e audit tuple completo. Os Application services recebem o `AuthenticatedExternalActor`, aplicam `webhooks:admin` internamente e vinculam autoria à intenção. Repositórios persistem command junto do recurso/idempotency, efeito de replay ou transição CAS; challenge só persiste após prova externa válida, junto da ativação. Concorrência e response-loss exigem autoria e request idênticos, sem persistir token/chave/ciphertext. Os contratos locais e E2Es Prisma/HTTP foram ampliados, mas o PostgreSQL local estava indisponível e prova hospedada, deploy e aceite seguem pendentes.

Evidência local F0.036/FR-242 (containment) — comandos revisionados de suspend/revoke/kill switch já cercam state, operações canceláveis, outbox e audit identity na mesma transação. O hardening atual exige role humana `administrator` para scopes administrativos e para recovery durante containment, recusa role persistida desconhecida e impede o client autenticante de suspender/revogar a si próprio. Testes preservam kill switch próprio recuperável, bloqueiam recovery por reviewer e comprovam o erro de self-lockout antes da persistência. PostgreSQL/HTTP hospedado, deploy e aceite permanecem abertos.

Incremento local F0.036/FR-242 + F0.029/FR-236 (lifecycle de artifact) — `apollo.artifacts.lifecycle.transition` deixou de aceitar um client ID solto e exige o actor autenticado com `artifacts:write`. O fingerprint inclui o context hash; a transação revision-fenced persiste o tuple completo junto da transição, idempotência e outbox, e a hidratação recalcula a identidade antes de aceitar replay. Credential diferente não herda o replay e adulteração falha com conflito de persistência. A migration remove rows e replay responses históricos sem atribuição confiável. PostgreSQL/HTTP hospedado, deploy e aceite permanecem pendentes.

Incremento local F0.036/FR-242 (download grants) — issue/revoke exigem `AuthenticatedExternalActor` com `artifacts:read`; emissão vincula idempotência ao context hash e revogação terminal grava seu próprio tuple na mesma atualização. Replays por outra credential/member são recusados e a hidratação de ambos os atores falha fechada após adulteração. A migration invalida grants curtos pré-contrato. O E2E final-export prepara a prova real de emissão, transferência, revogação e negação posterior, mas PostgreSQL/HTTP hospedado, deploy e aceite ainda pendem.

Incremento local F0.036/FR-242 (media upload) — `begin`, emissão de sessão, registro de parte, conclusão e aborto exigem o actor completo com `media:write` e gravam uma entrada imutável junto da mutação. O ponteiro da sessão corrente é revalidado contra workspace, upload, ação e actor; uma sessão substituída é negada antes do storage. Replay terminal por outra credential/member e ledger adulterado falham com conflito. T-FR-242 cobre o serviço e há teste PostgreSQL real registrado no CI, ainda não executado no gate hospedado; deploy e aceite permanecem pendentes.

Incremento local F0.036/FR-242 (asset rights) — PUT público aplica `artifacts:rights` no Application boundary e cada revisão persiste `AssetRightsChange` na transação que cerca CAS, snapshot e ponteiro corrente. O change guarda base/resultado, snapshot, fingerprint e actor tuple completo; ações internas têm origem explícita. Reuso de snapshot não reutiliza autoria: outra credential cria nova revisão, enquanto replay exato converge. Ledger ausente ou adulterado falha fechado. O teste PostgreSQL real está registrado no CI, ainda não executado no gate hospedado; deploy e aceite permanecem pendentes.

Incremento local F0.036/FR-242 (LUTs) — importação, próxima versão imutável, status e padrão revisionados do workspace exigem o actor completo com `projects:write`; seleção por projeto usa o mesmo boundary e persiste a atribuição no `V2EditCommand` já responsável por Command/ProjectVersion/impact. Diretor/sistema continuam internos; api-client sem audit completo é recusado. Context hash participa do fingerprint, hidratação o recalcula e outra credential falha fechado. O reset remove LUTs antigos e comandos externos anteriores sem tuple não são backfilled. Testes locais cobrem persistência, replay cross-credential e adulteração; o E2E HTTP/PostgreSQL lê as linhas reais e prova redaction, mas execução hospedada, deploy e aceite permanecem pendentes.

Incremento local F0.036/FR-242 + F0.037/FR-243 (operações auditáveis) — materialização e os enqueues de render, ingest, proxy, export, Diretor, source cleanup e long-form exigem ator completo e persistem o tuple canônico em `PublicOperation`; aggregates específicos compartilham o mesmo context hash. Cancel/retry efetivos criam `PublicOperationControlCommand` com intenção e transição na mesma transação do estado/outbox, e convergência não duplica ledger. A migration mantém linhas pré-contrato sem backfill, mas a aplicação as rejeita. T-FR-242 cobre binding, adulteração e cardinalidade; E2Es PostgreSQL/HTTP foram preparados, porém o PostgreSQL local não estava ouvindo. Execução hospedada, deploy e aceite permanecem pendentes.

Incremento local F0.036/FR-242 (produção em lote) — as 13 mutações públicas de batch, retry, alignment, takes, compatibility, variant e batch edit carregam o `AuthenticatedExternalActor` até sete repositories, reaplicam `projects:write` e usam o context hash em fingerprints, replay e fencing de preflight. Onze aggregates/commands persistem o tuple completo; hidratação, recovery de concorrência e commit por outra credential/member falham fechados. `T-FR-242` enumera rotas, serviços, ports e adapters para impedir regressão a client ID solto. Quatro jornadas PostgreSQL/HTTP foram ampliadas para ler os tuples de batch/action, alignment/review, library/selection, compatibility/recipe/portfolio e edit preflight/command, ainda sem execução por ausência do PostgreSQL local. Deploy e aceite permanecem pendentes.

Incremento local F0.036/FR-242 (inteligência do projeto) — nove mutações públicas de catálogo de fala/evidência/validação, busca semântica, desconstrução de source e contaminação recebem o ator autenticado, reaplicam `projects:write` e vinculam fingerprint/replay ao context hash. Seis repositories persistem o tuple completo em nove aggregate heads e recusam linha ausente, adulterada ou recuperada por outra credential/member. A migration mantém o grupo nullable apenas para não fabricar autoria histórica; o runtime falha fechado. `T-FR-242` enumera rotas, serviços e adapters, e as jornadas PostgreSQL/HTTP de desconstrução e contaminação foram preparadas sem execução local. Deploy, aceite e famílias produzidas por workers permanecem pendentes.

Incremento local F0.036/FR-242 + F0.027/FR-233 (Commands editoriais externos) — seis famílias de mutação sobre remoção de fala, transcript, edição manual, review patch e comparação preservam o actor autenticado completo da rota ao `V2EditCommand`, sem ledger paralelo. Application services reaplicam `projects:write`; fingerprint/replay incluem context hash; seis adapters reidratam credential/delegação e recusam adulteração ou troca de credential. A migration corretiva substitui a constraint para exigir `IS NOT NULL` explícito nos campos externos e não faz backfill. `T-FR-242` enumera rotas/services/adapters, a suíte local aprovou 1.094 testes e o golden real replacement → Diretor → worker → FFmpeg aprovou 2/2. Os E2Es PostgreSQL de edição manual e batch review estão preparados e skip-gated por ausência de banco isolado local; execução hospedada, deploy, aceite e provenance de Commands internos do Diretor permanecem pendentes.

Incremento local F0.036/FR-242 (avaliação do projeto) — onze aggregate heads de color pipeline, extração contígua, prova, proxy review, qualidade, asset selection, MVP core gate e validation envelope recebem e persistem a identidade autenticada completa. Onze rotas, dez Application services/repositories e o gate `T-FR-242` provam scope, workspace, context hash, replay e hidratação fail-closed. A migration `20260805040000_project_evaluation_actor_audit` cria os tuples e índices sem backfill de identidade. A suíte local aprovou 1.095 testes e o schema 145 tabelas/750 índices/553 FKs; PostgreSQL real, implantação e aceite permanecem pendentes.

Incremento local F0.036/FR-242 (análise direta e durável) — Diretor, catálogo long-form e processamento hierárquico compartilham um contrato de execução que separa a identidade autenticada iniciadora da proveniência operacional. Requests diretos exigem o actor completo e `projects:write`; stages duráveis exigem o audit tuple já persistido e lineage exata de operation/workflow/stage/checkpoint. Fingerprints e replays são credential-bound, o Command do Diretor e os dois run heads persistem o tuple, e repositories revalidam FKs, fences e hashes antes de publicar. A migration `20260805050000_project_analysis_execution_audit` fecha a semântica `NULL` dos checks sem inventar autoria histórica. Provas locais e integração controlada de fencing passam; PostgreSQL real, execução hospedada, deploy e aceite permanecem pendentes.

Incremento local F0.036/FR-242 (análises derivadas long-form) — diarização, cinco produtores de evidência contígua e avaliação preservam o audit context iniciador até os três run heads, junto da provenance exata de operation/workflow/stage/checkpoint. Os dois ports contíguos não expõem persistência sem lease; serviço, adapter e operação corrente recusam drift de workspace, credential, stage ou fence, e replay é context-bound. A migration `20260805070000_long_form_derived_execution_audit` usa grupos all-null ou completos com FKs e índices, sem backfill de autoria; o runtime recusa o grupo histórico. Testes locais e adapters controlados passam; o E2E PostgreSQL foi ampliado, mas execução isolada, deploy e aceite permanecem pendentes.

Evidência preparada F0.036/FR-242 (security E2E) — a jornada HTTP/PostgreSQL existente foi ampliada para falsificar empréstimo de scopes administrativos do client UI a um reviewer, self suspend/revoke sem persistência de command, credential expirada sem atualização de `lastUsedAt` e persistência exata dos audit tuples Bearer/UI nos comandos de containment. Ela conserva as provas anteriores de capability filtering, ocultação cross-workspace, overlap zero, token substituído e revogação terminal. A execução hospedada desta ampliação, deploy e aceite permanecem pendentes; por isso a microtarefa continua aberta.

## Verificação

- Todo `FR-*` do PRD deve aparecer exatamente uma vez nesta matriz.
- CI documental deve falhar se um ID estiver ausente ou duplicado.
- Mudança de fase/spec exige atualizar esta matriz no mesmo commit.
