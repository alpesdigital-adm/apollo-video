import test from 'node:test';import assert from 'node:assert/strict';import {automationAbuseRisk,canReleasePhase,costRiskGate,integrityRisk,libraryGovernance,PHASE_GATES,providerRisk,REFERENCE_DATASETS,RELEASE_DECISIONS,storageGarbageCollection,validateReleaseDecisionRecord} from '../../src/v2/application/release-risk-control.ts';
test('T-RISK-001..011 every operational risk has a fail-closed executable control',()=>{assert.equal(PHASE_GATES.length,6);assert.equal(canReleasePhase('F3',new Set(['F0','F1'])).allowed,false);assert.equal(REFERENCE_DATASETS[0].rubricVersion,'1');assert.equal(costRiskGate({preflight:true,reserved:20,runCap:10,workspaceSpent:0,workspaceCap:100,cacheChecked:true,deduped:true,topN:5,requested:5}).allowed,false);assert.equal(providerRisk({contractPassed:true,healthy:false,circuitOpen:true,current:{capability:'x',price:1,limit:1,version:'2'},previous:{capability:'x',price:1,limit:1,version:'1'},fallbackAvailable:true}).fallback,true);assert.equal(integrityRisk({before:true,afterReorder:true,afterTrim:true,afterLocalization:false,qualifiers:true,dependencies:true}).passed,false);assert.equal(libraryGovernance({checksum:true,rights:true,metadataTier:true,workspaceScoped:true,lifecycle:true,duplicates:1,unknown:2,unused:3}).passed,true);const gc=storageGarbageCollection({assets:[{id:'m',class:'master',ageDays:1000,reconstructable:false,references:[]},{id:'d',class:'derivative',ageDays:40,reconstructable:true,references:[]}],quotas:{master:1,derivative:1,temp:1},retention:{master:365,derivative:30,temp:1}});assert.equal(gc.find(x=>x.id==='m').eligible,false);assert.equal(gc.find(x=>x.id==='d').eligible,true);assert.equal(automationAbuseRisk({requestsPerMinute:999,mutationsPerMinute:1,generationSpend:1,downloadsGb:1,limits:{requests:100,mutations:10,spend:10,downloadsGb:10},scopesMinimal:true,credentialRevocable:true,killSwitch:false}).action,'suspend-client-and-investigate');});
test('T-DECISION-001 release decisions fix permissions, thresholds, retention, API, batch, providers and audio defaults',()=>{assert.equal(RELEASE_DECISIONS.beforeF1.apiSupport,'major + 180d sunset');assert.equal(RELEASE_DECISIONS.beforeF2.batchMax,50);assert.ok(RELEASE_DECISIONS.beforeF3.providers.includes('Higgsfield pilot after contract suite'));assert.equal(RELEASE_DECISIONS.beforeF5.loudness.socialLufs,-14);assert.equal(validateReleaseDecisionRecord({owner:'platform',due:'2026-07-17',options:['a'],evidence:['test'],impact:'public',adr:'ADR-140',configVersion:1,tests:['T']}).valid,true);});
