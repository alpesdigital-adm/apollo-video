import test from 'node:test';import assert from 'node:assert/strict';import {addManualPlaybackAnchor,buildReactPlaybackMap,mapReactSessionToReference,multicamLongformGate,REACT_PLAYBACK_FIXTURE} from '../../src/v2/domain/react-playback-map.ts';
test('T-F4-015 models play, pause, rewind and seek piecewise without equal durations',()=>{const pieces=buildReactPlaybackMap(REACT_PLAYBACK_FIXTURE);assert.deepEqual(pieces.map(x=>x.mode),['play','pause','rewind','seek','play']);assert.equal(mapReactSessionToReference(pieces,2500).referenceMs,2500);assert.equal(mapReactSessionToReference(pieces,8500).anchorRequired,true);const hidden=addManualPlaybackAnchor([], {sessionMs:1000,referenceMs:500});assert.equal(hidden[0].signal,'manual-anchor');});
test('T-F4-GATE approves unequal podcast/teacher/react/long-form only with sufficient written evidence',()=>{const pieces=buildReactPlaybackMap(REACT_PLAYBACK_FIXTURE);const result=multicamLongformGate({podcastConfidence:.9,teacherScreenConfidence:.85,insufficientCase:true,reactPieces:pieces,activeSpeakerShots:4,demonstrationShots:3,multiRangeDurationMs:120000,contextPreserved:true});assert.equal(result.passed,true);assert.deepEqual(Object.values(result.evidence),[true,true,true,true,true,true]);assert.equal(multicamLongformGate({...{podcastConfidence:.9,teacherScreenConfidence:.85,insufficientCase:true,reactPieces:pieces,activeSpeakerShots:4,demonstrationShots:3,multiRangeDurationMs:120000},contextPreserved:false}).passed,false);});
